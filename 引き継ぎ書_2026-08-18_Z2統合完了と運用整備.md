# 引き継ぎ書 — 2026-08-17〜18 Z2統合(M1〜M5)完了と運用整備

> 書き手: ディレクター(Fable)。読者: **次のセッションで立ち上がるエージェント**。
> 記録日: 2026-08-22(8/17〜18の作業+8/19〜22の運用実績を含めて総括)。
> 背景の詳細は CLAUDE.md(憲章・知見)、前回分は 引き継ぎ書_2026-08-11_再起動後の再開手順.md。
> **鉄則: エージェントの完了報告は信用しない。ファイル実在・grep・実行で必ず実地検証。**

---

## 0. 3行サマリ

1. **Z2統合は完了し、本番運用が始まっている**(M1スパイク12/12合格・M5実機一度押しでdelivered到達。
   8/22朝時点で delivered 34件 / fuda 28件 / queued 16件 / verifying 1件 = ユーザーが実際に使っている)
2. Z2のSTEP取り込み永久ハングの真因は **3D Interconnect設定ON** だった(OS劣化説・ハング体質説は
   Z2に関しては誤診)。エンジン側で恒久対策済み(自動OFF+SaveAs3フォールバック=368e48e)
3. Z2は**毎朝05:30にジョブ待ちゲート付き自動再起動**(Z2-DailyReboot)。8/22朝にゲートの
   cp932クラッシュを発見・修正済み(§4)

## 1. 現在地(2026-08-22朝・実測)

| 項目 | 状態 |
|---|---|
| DGA(本リポジトリ)main | `d4c034a`(GitHubへpush済み・作業ツリークリーン) |
| SOLIDIFY(姉妹)main | `a36972e`(ローカルのみ。Z2へはbundleで適用済み) |
| Z2の DGA clone | `C:\workshop\Drawing-Generator-Agent` = **368e48e**(エンジン修正まで。c6acf9b以降はドキュメントのみなので未転送でOK) |
| Z2の SOLIDIFY | **a36972e**(v3.6+STEPのみ受付+工番マスタ+UI改善まで全部入り) |
| Z2退避ブランチ | DGA: z2-snapshot-20260817 / workshop: z2-snapshot-20260817(=1308ac4)・20260818(=45929ce) |
| Z2のSW2021設定 | **3D Interconnect=OFF(永続化済み)**・図面テンプレpref10設定済み |
| 工房ヘルス | ok(8/19〜21は毎朝再起動成功。8/22はゲート不具合でスキップ→修正済み) |
| 接続経路 | 社内LAN=`ssh z2` / **自宅=`ssh z2-ts`(Tailscale・2026-08-18ユーザー追加)**。手順は同一 |

❗**姉妹リポジトリの作業ツリーに当セッション外の変更あり**: `.gitignore` が modified、
`engine/koban_master.py` が untracked でコピーされている(誰がいつ置いたか不明)。
**勝手に消さず、今泉さんに確認してから**扱うこと。

## 2. 2026-08-17にやったこと(Z2統合デー)

### 2-1. M1: Z2配置とSW2021 go判定 — **12/12合格**

- DGAをZ2へ初回配置(bundle・手順書どおり)。依存は全て導入済みだった(ezdxf 1.4.4一致)
- 初回スパイクは3/12 → 長い診断の末に真因特定(経緯の詳細と全知見は
  **CLAUDE.md知見「❗❗Z2実機M1合格(12/12)」の節**を必読。要点だけ:)
  - **真因: `swMultiCAD_Enable3DInterconnect`(toggle 691)=ON だと LoadFile4 のCOM戻りが
    永遠に失われる**(画面上は取り込み完了・フィーチャーツリーに `->` 矢印が出るのがサイン)
  - 診断過程の「接種5連敗」「OS再起動でも再発」はすべてこの設定が原因。**Z2はハング体質ではなかった**
  - 劣化した長期稼働SWインスタンスは OpenDoc6/LoadFile4 が**偽の型不一致エラー**を出す(新品では出ない)
- **エンジン恒久対策(コミット368e48e)**: ①`open_step_readonly`がインポート前にInterconnectを
  自動OFF(戻さない) ②`export_dxf`はExtension.SaveAs3が型不一致の版で旧`IModelDoc2.SaveAs3`
  (3引数)へフォールバック(戻り値は信用せずファイル実在が正)
- 図面テンプレ: `C:\ProgramData\SOLIDWORKS\SOLIDWORKS 2021\templates\図面.DRWDOT` をpref10へ設定
- 結果の正本: `調査/z2統合_M1_SW2021スパイク_Z2実機.md`(開発機基線と同一手順)
- Z2固有の実測差分: ScaleDecimal勝手値=5.0(開発機2.0)/ インポート部品GetTitle=拡張子なし'1-18'

### 2-2. M5: SOLIDIFY v3.6適用と実機一度押し — **delivered到達**

- workshop 1308ac4→0449a86 をff適用、Z2上でテスト実行
  (drawing系5本+real_pipeline=**全緑**。test_drawing_promptsのNG1件は「コミット前に回す設計の
  テストがコミット後は必然的にNGになる」時系列アーティファクトで実害なし)
- アプリ/ワーカー再起動は**見張り(SOLIDIFY-Watchdog)経由**で成功(殺すと5分以内に蘇生。
  SW本体も start_workshop.ps1 が蘇生させることを実測確認)
- **実機一度押しの記録**:
  - J20260817-001/002: SLDPRT投入で計測段failed(SW2026保存は開けない。**失敗経路が契約どおり
    丁寧に動くことを確認**)
  - **J20260817-003(1-03.STEP): 全区間完走** — 計測→AI向き選択→AI作図計画→**質問票(fuda)3件**
    (φ16六角突起=M16ねじか等。「3Dが語らないものを描き足さない」原則どおり)→ユーザー回答→
    作図→**検査不合格(中心線が表題欄へ0.75mm侵入)→自動作り直し→2回目で全ゲート合格**→
    納品→`\\lineworks-sv\...\3D納品` へ配達成功。**作り直しループと配達の実機初実証**

### 2-3. ユーザー裁定による改修(SOLIDIFY側・全てZ2適用済み)

- **STEPのみ受付(3c4c196)**: 図面化依頼はSLDPRT等を版に関係なく400で断る(⚠すり抜け事故を受けた
  裁定)。実装契約v3.6追補の入力(A)を改定済み。受け入れ=`test_drawing_intake.case_step_only`
- **工番マスタ連携(45929ce)**: 受付フォームに工番欄(任意)→`GET /api/koban/{工番}`で
  納入先/製品名を参考表示+**装置名が空なら製品名を自動記入**。実装は`app/koban.py`=
  **CSV直読み**(workers.pyと同じ実績パターン・認証/トークン不要・副作用なし。
  手順書のGAS方式はrefreshMasterが書き込みを伴うため採用せず)。㈱→(株)正規化は裁定採用。
  受け入れ=`case_koban`(実シート930行でも検証済み)
- DGA側には手順書どおりのGAS版 `engine/koban_master.py` も検証済みで存在(11/11合格・
  `.env`にトークン設定済み・コミット5510a1e)。**表題欄への結線はまだ**(§6)

## 3. 2026-08-18にやったこと(UI改善と物理トラブル解決)

- **UI: 選択中ボタンの朱塗り表示(7f7611c)+ホバー効果(a36972e)** — 依頼種別切替・
  選びかた4ボタン等。`aria-pressed`ベース。index.htmlは毎リクエスト読み込みなので再起動不要で反映
- **Z2物理モニタ問題を解決**: 真因は**モニタ(iiyama PL2493H)のDisplayPort入力口の故障**。
  HDMIなら映る。Z2本体・A400・iGPU・BIOSは全ポート無罪(切り分けの決め手は「同じモニタの別入力」。
  EDIDだけ通って映像線が沈黙する故障はPC側問題と酷似するので注意)
- **Z2-DailyReboot(毎朝05:30・ユーザー新設)をゲート方式に改修**: `C:\workshop\z2_reboot_gate.py`
  が工房DBをread-onlyで確認し、作業中(reading/verifying/building/inspecting)0件になってから
  `shutdown /r`。90分待って空かなければ**スキップ**(ユーザー裁定)。DB不読はfail-open。
  SOLIDIFYの**中断復帰**(起動時に作業中ジョブをqueuedへ戻す契約機能)が保険として下支え

## 4. 事故記録(2026-08-22発見・修正済み)

**ゲートのcp932クラッシュ**: 8/19〜21は「0件→即再起動」で正常動作したが、8/22朝に初めて
「待機」パスへ入った際、ログ文中の全角ダッシュ「—」がcmdリダイレクト先のcp932で
UnicodeEncodeError → **ゲートごとクラッシュ(結果コード1・再起動されず)**。実害はスキップ方向
だったが設計どおりではない。**修正済み**(stdoutのUTF-8化+ダッシュ除去。稼働中の工房に対する
`--check`で待機パスの正常動作を実証)。教訓: **cmd経由で走るPythonは必ず
`sys.stdout.reconfigure(encoding="utf-8")` を入れる**(他のスクリプトには入れていて、これだけ漏れた)。

## 5. Z2に常設したツール(消さないこと)

| 何 | どこ | 用途 |
|---|---|---|
| 再起動ゲート | C:\workshop\z2_reboot_gate.py/.cmd + z2_reboot_gate.log | Z2-DailyRebootタスクの実体 |
| 接種ハーネス | z2_warmup_wrap.py + z2_retry_import.py + da_warmup.cmd(タスクDA-WARMUP) | SW取り込み健康診断(90秒タイムボックス) |
| M1スパイク | da_spike_launcher.py + da_spike.cmd(タスクDA-SPIKE) | 12検証点の再実行 |
| 残骸掃除 | z2_cleanup2.py | 未保存インポート部品のQuitDoc |
| SW設定 | z2_setpref.py(タスクDA-SETPREF) | pref10/Interconnect確認 |
| DB覗き | z2_dbcheck.py / z2_jobdetail.py | ジョブ状況の読み取り専用確認 |

Z2運用の作法(CLAUDE.md知見にも記載): sshインラインの引用符は剥がれる→**スクリプトをscpして実行**。
COMは**schtasksの対話タスク(/it)経由**。モニタ無しでも対話タスクからスクリーンショット取得可。

## 6. 残タスク(優先順)

1. **fuda 28件の回答**(ユーザー)— 本格利用開始に伴い赤い札が溜まっている。画面から回答を
2. **§1-3(SW COM堅牢化のSOLIDIFY実装)の再定義**: Interconnect真因潰しで緊急度は下がった。
   残る価値があるのは ①計測タイムアウト→SW作り直し ②ジョブ間の実インポート健康診断
   ③残骸掃除、の3ガードを `phase2/worker/drawing2d.py` へ。**本番運用の失敗実績を見てから
   必要性を判断**するのが良い(現状8/17以降エンジン起因の失敗は0)
3. **工番マスタのDGA表題欄への結線**(工番→装置名セルは受付で自動記入済み。それ以上の
   結線をするかはユーザーと相談)
4. **M5の残り確認**: 「止める」ボタンの実機一度押し / `ledger_export` の人間可読出力確認
5. 開発機側の宿題: **1-106質問票への回答待ち** / ACROVA GMMでの生成DXF開図確認 /
   データ疑義リスト10件超 / 1-29図面不存在の件 / SW2023 SP0.1→SP5系アップデート提案(mfc140u対策)
6. 姉妹リポジトリの未コミット変更(§1の❗)の扱いをユーザーに確認

## 7. 主要ファイルの地図(今回分)

| 何 | どこ |
|---|---|
| Z2/SW2021の全知見 | CLAUDE.md「❗❗Z2実機M1合格(12/12)」節 |
| M1スパイク結果(Z2実機) | 調査/z2統合_M1_SW2021スパイク_Z2実機.md |
| エンジンのZ2互換対応 | engine/draw_pipeline.py(368e48e: toggle 691自動OFF+SaveAs3フォールバック) |
| 工番マスタ(DGA/GAS版) | engine/koban_master.py + 工番マスター連携_実装手順書.md + .env(コミット禁止) |
| 工番マスタ(SOLIDIFY/CSV版) | 姉妹 phase2/app/koban.py + server.py `/api/koban` + index.html |
| STEPのみ受付の契約 | 姉妹 phase2/実装契約.md v3.6追補の入力(A)改定+工番マスタ追記 |
| bundle作成ツールの既知バグ | make_z2_bundle.ps1 は `-RepoDir` 指定時にverifyを呼び出し元リポジトリで
  実行してしまい失敗する(bundle自体は正常)。姉妹リポジトリは手動 `git bundle create/verify` で運用 |

## 8. 次のエージェントへの警告(実事故由来)

1. **完了報告は検証してから信じる**(従来どおり)
2. **Z2のSWインスタンスは殺してよいか毎回確認**: 見張りが蘇生させるが、実行中ジョブ0の確認が先
3. **ゲート/タスク系のPythonはstdoutのUTF-8化を忘れない**(§4の事故)
4. **「EDIDが通る=接続正常」ではない**(モニタDP口故障・Interconnectハング、いずれも
   「半分だけ通る」壊れ方だった。切り分けは同一経路・同一入力の比較で)
5. 診断で仮説が外れ続けたら、**動いている実績側(姉妹ワーカー・他のZ2マシン)との差分**を探すのが近道
