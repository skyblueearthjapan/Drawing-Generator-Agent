# Drawing-Generator-Agent — 3Dモデル→部品図DXF自動生成システム

> 目的: 3Dモデル(SLDPRT/STEP)を渡せば、**そのまま出図できるレベルの部品図DXF**を出す仕組み。
> 最終ゴール: 「3Dデータを投入 → 図枠・寸法・注記の入った正確な部品図 → 紙で外注先・機械加工屋・材料屋へ渡してそのままものづくりできる」ループの自動化。
> 思想(姉妹プロジェクト「3D CAD Operator Agent」から継承): 推論はAI、品質は決定論的な検証ゲート、最終判断は人間。
> 「加工者・調達者が困る図面を1枚でも出したら信頼を失う」— 検証ゲートを通らない限り納品しない。

## 体制(2026-08-09 ユーザー決定)

- **ディレクター**: Fable — タスク分解・指示出し・成果物レビュー・検証ゲート判定・責任
- **実作業**: サブエージェント(Opusメイン。コスト削減のためSonnetを使える所に積極投入)
- 修正・改修の必要が出たらユーザー(今泉さん)がその都度対応。フェーズ0〜5は一気通貫で進める許可済み

## アーキテクチャ(3層+ゲート)

1. **AIオペレータ層**(Claude): 3Dモデル解釈 → 形状クラス判定 → 作図計画
   (ビュー選択・配置、寸法計画=どの寸法を・どのビューに・どの基準で入れるか)。
   不明点は勝手に仮定して納品せず「質問票」を出す
2. **作図実行層**(`engine/`): Python + pywin32 で SolidWorks 2023 をCOM操作(投影・計測)+
   ezdxf で最終DXF合成(図枠・寸法・注記)
3. **検証ゲート**(必須・AIの自己申告を信用しない):
   - ゲート① **寸法値照合**: 出力DXFをezdxfで読み戻し、全DIMENSIONの値を3Dモデルの実測値
     (SW計測API)と独立に突き合わせ
   - ゲート② **寸法完全性**: その図面だけで部品形状が一意に決まるか(過不足チェック)。
     加工者が電卓を叩かず作れることが基準
   - ゲート③ **目視照合**: PNG化して、ビューの正しさ・注記の重なり・線の欠落を確認
   - ゲート④ **人間図面との比較**(開発期間の特権): 参考ペア約200組で人間作図と突き合わせ、
     自社流儀の再現度を採点
   - 不合格 → 作り直しループ。解決不能 → 人間へ質問票
4. **納品**: `生成図面/` へ DXF+プレビューPNG、`解釈レポート/` へ解釈レポート、`台帳.md` に記帳

## 仕様(2026-08-09 ユーザー決定)

- **成果物は DXF**(自社図枠付き部品図)。紙出力して外注先へそのまま渡せる品質
- SolidWorks図面テンプレートは存在しない → 参考DXFの図枠から新規作成する
- 表題欄: **品名=3Dモデル名 / 重量=SW質量特性から自動算出** / **図番・材質・個数=依頼時入力**
- 寸法記入は設計手法・JIS製図規格・自社流儀(参考200組から抽出)をベースにルール化する

## 設計判断(v1・フェーズ2で実証して見直す)

- **ハイブリッド方式**: SolidWorks COM は「3D→2D投影(ビュー生成)」と「計測(質量特性・寸法実測)」に使い、
  **最終DXFの合成(図枠・寸法・注記の配置)は ezdxf で行う**。
  理由: (a)図枠は参考DXFのエンティティをそのまま流用でき見た目が完全一致
  (b)寸法の書式・配置をSW APIの癖と戦わずに完全制御できる
  (c)検証ゲートが同じezdxf座標系で閉じる。
  SWネイティブ図面(SLDDRW)方式はフォールバックとして温存
- 参考データ`荏原トライ調整用/`は容量(約200MB)のためgit管理外(ローカル保持)

## 参考データ(正解ペア約200組)

```
荏原トライ調整用/
├─ 3D/<軸>/            SLDPRT(製作部品: 例 1-03.SLDPRT)+購入品+SLDASM
└─ DXF/部品表用DXFデータ/<軸>/  図枠付き正式部品図DXF(例 25154-1-03_ジャッキプレート.dxf)
```
- 対応は図番: `3D\1.走行軸\1-03.SLDPRT` ↔ `25154-1-03_ジャッキプレート.dxf`(工番25154-軸-連番)
- DXFはAC1015、作図範囲840×594(Ａ３表記×用紙倍率2)。1枚に複数子部品(03-1,03-2…)併記あり
- **社内2D CADは日立産機システム ACROVA GMM**(2026-08-09ユーザー回答)。`DXF/<軸>/*.ddw` は
  そのネイティブ形式(直接は読まない)。参考DXFはACROVA GMMのDXF出力実例であり、
  文字スタイル名 GMM0xx はGMM由来。**生成DXFはこの書式(AC1015/cp932/GMMスタイル)を踏襲すれば
  互換リスク最小**。最終確認として「生成DXFをACROVA GMMで開いて崩れない」ことの人間検証を
  フェーズ4の受け入れ項目に入れる

## 環境(このPCで実証済み)

- **SolidWorks 2023 = ProgID `SldWorks.Application.31`**。接続は必ず `engine/sw_compat.py` の
  `connect_sw()` / `gen_module()` 経由(姉妹リポジトリから流用。マシン差を自動判別)
- Python + pywin32、`GetActiveObject` のみ有効(New-Object/無印ProgIDは失敗する)。
  **SolidWorksが起動していないと接続できない**
- COM操作は sandbox 不可 → PowerShellツールで `dangerouslyDisableSandbox: true` で `python script.py` 実行
- DXF読み書きは ezdxf 1.4.4(インストール済み)
- SW COMの膨大な罠(gen_pyラッパー、プロパティ/メソッド差異、byref引数等)は
  姉妹リポジトリ `C:\Users\imaizumi.LINEWORKS-NET\Documents\3D CAD Operator Agent\CLAUDE.md` の
  「SolidWorks API」各節を必ず参照(図面API固有の知見は本ファイルに蓄積していく)

## フォルダ構成

```
engine/          恒久エンジン(sw_compat.py, sw_helper.py, dxf_dump.py, dxf_render.py, dxf_geo.py=姉妹リポジトリ由来)
図枠/            図枠テンプレート(抽出図枠DXF、フィールド定義、DIMSTYLE採取物)
調査/            分析スクリプト・調査結果(フェーズ1/3の中間物)
生成図面/        納品物(DXF+プレビューPNG)
解釈レポート/     部品毎の解釈レポート(仮定・矛盾・検証結果)
台帳.md          処理台帳
```

## ロードマップ

- **フェーズ0**: リポジトリ・環境整備(engine流用、SW COM疎通、憲章) 
- **フェーズ1**: 図枠解析とテンプレート化(共通エンティティ抽出→図枠テンプレート+フィールド座標マップ)
- **フェーズ2**: パイプライン貫通(1部品: SLDPRT→SWビュー投影→DXF合成→検証)
- **フェーズ3**: 寸法記入エンジン(最難所。自社流儀の抽出→作図計画→寸法を1本ずつ配置)
- **フェーズ4**: 検証ゲート整備と量産検証(走行軸部品群で人間図面と比較反復)
- **フェーズ5**: システム化(依頼受付→バッチ→台帳のループ自動化)

## 正面ビュー選定の確定戦略【ディレクター裁定・2026-08-10・60部品の実測に基づく】

決定論ルールの改良路線は**打ち切り**(v1=38.5%、v2=対照と同率53.8%。旋盤物特化も独立データで引き分け)。
1. **既定の単一推定は「STEPの座標系そのまま(恒等)」**(56%。教師モデルは正面XYスケッチ流儀)
2. **上位6候補(形状クラス別の事前分布順)を投影→PNG→AIオペレータが目視選択**(人間の選択の90%を被覆。
   人間の正面は50/50全部が「SW標準6ビュー×90度回転」内=候補は有限で閉じている)
3. 品質はゲート①②が選択後に決定論で保証する(向き選択をAIに委ねても検証は崩れない)
4. candidates が拮抗して決めきれない場合は人間へ質問票

## ❗図面の「見やすさ」はゲート合格と別の品質軸【ユーザー指摘・2026-08-10】

ユーザー(今泉さん)の指摘: 自動生成図面は「寸法の取り方が見にくく、毎回同じパターン。
人間の図面と違う。中心線(穴のクロス等)も無くなった」。ディレクターが人間図面と並置確認して
言語化した欠落5点:
1. **中心線の不在**(穴ごとのクロス・ビューを貫く軸中心線・PCD参照円・旋盤軸線)= 図面の骨格
2. **穴位置の角度寸法(円弧)表現の不在**(PCD注記の文字だけでは「見て分からない」)
3. **寸法の機械的スタック**(径=全部左、長さ=全部下、と毎回同型)vs 人間の機能別分散配置
4. **機能面への「はめあい公差+仕上げ記号▽▽+C面取り」セットの不在**
5. **少数精鋭感の欠如**(ゲート②網羅を優先し注記へ逃がす判断が無い)
- TEST-002/003が上品だったのは人間の寸法セットを写したから。**様式知識の機械化が必要**
- ゲートに合格した図面でも「読み手への配慮の層」が無ければ出図品質ではない。
  中心線エンジン+様式規則(plan_prompt)+様式スコア(ゲート④拡張)で対処中

## 様式品質の到達点【ユーザー裁定・2026-08-11】

様式サイクル3弾(中心線・配置バランス・3層構造)の結果を確認したユーザーの裁定:
「人がその時々に目視で寸法位置・ビュー配置を調整している部分が大きく、決まったルールにするのは
難しい。**できる範囲としては今が最大限改善された状態。とりあえずはこれでいい**」
- 図面ごとの微調整(寸法の置き所・ビュー左右の入替等)は**人の裁量領域**として深追いしない
- 様式の機械化はここまでの実装(27論点のうち機械化可能な範囲)を維持水準とする

## 2ビュー図面の正面図位置【ユーザー裁定・2026-08-10】

**左が正面図(JIS標準どおり)**。盲検第2弾の「右が正面図」読みは誤りで、同弾の向き厳密正解率は
1/8に訂正される。含意:
- ビュー役割推定(compare_views.assign_roles)・採点は「左=正面図」を正とする
- AIオペレータの向き選択は依然最大の弱点(候補被覆100%なのに選択が人間とずれる)。
  次の改善は「人間ならどれを顔にするか」の選択規準の学習(参考図面の正面図の傾向分析)
- 向きが人間とずれても図面自体はゲートが保証する(スタイル課題であり正確性課題ではない)

## 運用ルール(毎回必ず)

- 作図前に: 3Dモデルの質量特性・バウンディングボックス・フィーチャー構成を取得して形状クラスを判定
- 仮定・モデル不備・自動判断は全て解釈レポートに明記
- 完了したら台帳に1行追記。新しい知見はこのファイルの「知見」節に蓄積
- サブエージェントの成果物はディレクターがレビューしてから次工程へ渡す(自己申告を信用しない)

## 知見(蓄積していく)

### ❗続報: 「虚偽報告」の正体は「早すぎる完了報告+通知後も稼働継続」だった【2026-08-10・TaskStopで確定】
上記事故のエージェントは実は**完了通知の後も生き続けて実装を書き続けていた**(停止時の出力
「Now the falsification tests...」で作業中と確定)。完了報告の時点でコードが無かったのは事実だが、
その後の実装は本物で、後続2エージェントが「同一ファイルへの同時書き込み」を検知して誠実に停止する
事態になった。幻の納品(1-04・15:49)もこの継続稼働コードの産物(main では再現不能→保留に降格)。
- **教訓1: タスク完了通知は「エージェントが終了した」証明ではない**(同一IDが複数回通知され得る仕様)。
  同スコープの後続エージェントを出す前に、必ず旧タスクを TaskStop してから出す
- **教訓2: 同一ファイル群は常に単独書き手**。並行エージェントのスコープはファイル単位で排他にする
- **教訓3: エンジン担当には作業前後の git status クリーン確認と「自分以外の書き込み検知で即停止」を
  義務化する**(後続2体はこれで混線被害を回避できた。誠実な停止報告は責めず賞賛する)
- 混線したコードは quarantine-ghost-e3e4 / quarantine-mixed-cycle2 ブランチに隔離済み

### ❗エージェントの完了報告が丸ごと虚偽だった事故【2026-08-10・ディレクター検証で検出】
エンジン拡張(E4/F1/E5/E3)担当エージェントが「実装完了・反証合格・納品2件追加・台帳掃除済み」と
詳細な最終報告を出したが、**実際にはワーキングツリーに一切の実装が存在しなかった**
(新関数なし・納品なし・レポートファイルなし。差分はTEST図面のタイムスタンプのみ)。
ディレクターが報告を信じて虚偽内容のコミットを作ってしまい、revertで訂正した(03a419c→6c5110d)。
- **教訓: コミット前に必ず「主張されたファイルの存在」「主張された関数のgrep」「主張された納品物」を
  最低1点ずつ実地確認する**。数値の綺麗な報告ほど疑う。憲章の「自己申告を信用しない」は
  検証ゲートだけでなく**エージェントの作業報告そのもの**に適用する
- 長時間・高トール数のエージェントは、途中のコンテキスト圧縮などで「やったつもり」の
  完了報告を出すことがある。成果物パスの列挙だけでは証拠にならない

### ❗荏原の3DモデルはSW2026保存 → 開発機SW2023では開けない【2026-08-09・フェーズ0で発覚】
- `sw.VersionHistory(path)` が `19000[2026/100]` を返し、`OpenDoc7` の `spec.Error=8192`
  (swFileLoadError_FutureVersion)。SolidWorksの互換は後方のみで、SLDPRTの旧版保存も不可能
- **対策**: SW2026のマシンで `ツール/STEP一括変換マクロ_SW2026マシン用.txt` を実行して
  「教師STEP」を作ってもらう(ユーザー依頼中)。STEPならSW2023で開ける。
  最終システムの入力も STEP/3D なので、**STEP入力前提の設計はゴールに合致する**
- 教師STEPが届くまでのパイプライン開発は、姉妹リポジトリの `生成3D/`(SW2023保存)を代用する
- **開けるかどうかは開く前に `VersionHistory` で判定できる**(開かずに済む)

### STEP取り込み【2026-08-10・荏原実部品5点で実証。詳細は 調査/step_import_report.md】
- **結論: STEPのジオメトリは完全に信頼できる**(5部品で人間図面と一致度1.0000・外形差0.009mm以下・
  鏡像なし)。ただし**STEPの座標系は部品ごとにバラバラ**なので「正面ビューの選定」は別機構が必要
  (円筒面の軸ベクトル集計→旋盤物=主軸を紙面水平/板物=板厚を紙面法線。5部品全部で正解)
- **❗STEPは `OpenDoc6` では開けない**(err=2097152/1024)。
  `isd=sw.GetImportFileData(path)` → `sw.LoadFile4(path,"r",isd,0)` が正解。単位はmmで正しく入る
- **❗インポート部品は `GetPathName()=''`** → `CreateDrawViewFromModelView3` には
  `GetTitle()`(例 '1-18.SLDPRT')を渡す
- **❗未保存インポート部品は `CloseDoc` では閉じない**(無反応で居座る)→ **`QuitDoc(title)`**。
  図面→部品の順で閉じる
- **❗`GetFirstDocument`+`GetNext` は1件目で切れる** → 開いているドキュメントの列挙は `GetDocuments()`
- **❗STEP由来B-repは円筒が半割り2面** → 継ぎ目の接線エッジが実在しない線として出る。
  **STEP入力では `SetDisplayTangentEdges2(0)` が既定**(「1が一択」はSWネイティブ限定。逆に注意)。
  フィレット持ち部品はON/OFF両方出して照合で選ぶ
- **STEPは材質を持たない**(密度=水1000)→ 重量は必ず依頼JSONの材質×SW体積で算出
- 人間図面の作図誤差3例目: 3-05内径が寸法文字φ30に対しCIRCLE実体φ30.1。SW計測はφ30.0000。
  **照合の正はあくまで3Dモデル実測**
- 図枠レイヤ名はファイルごとに違う('254'/'表題欄'等)→ subtract_frame は IGNORE_LAYER_DEFAULT=True
  (バケットA80枚で平均一致101.2→112.0、図枠が引けないファイル12→4枚)
- 尺度と用紙倍率は別物: `DXF1単位[mm] = (尺度分母/分子) / 用紙倍率`
- ゲート④合格基準案: 円直径差≤0.05mm/外形差≤0.05mm/人間側被覆1.0000必須/det=-1が最良なら即停止

### ❗正面ビュー選定: 形状主導ルールは「STEPの座標系そのまま」に負けた【フェーズ4・30部品で実測。詳細は 調査/phase4_scoreboard.md】
- **向き正解率: 形状主導ルールv1 = 44.0%(11/25) < 対照「STEPの座標系そのまま」= 60.0%(15/25)**。
  必ず**対照実験を併走**させること(`run_batch.py --baseline`)。ルールが「何もしない」に勝つ保証は無い
- **教師モデルは「正面(XY)にスケッチ→Zに押し出す」流儀で作られており、STEPがその座標系を保っている**
  → 人間の紙面法線がモデル±Zである確率 **68%(17/25)**、紙面水平が+Xである確率 **60%(15/25)**。
  step_import_report.md の「STEPの座標系は使えない」は5部品中1点(3-05)に引きずられた過大な一般化だった
- **「最小辺=板厚を紙面法線」は曲げ物ブラケット/ドグで裏目**(6件で対照に負けた)。板厚方向は紙面法線ではない
- **旋盤物だけは形状判定が勝つ**ように見えた(主軸X→恒等 / 主軸Y→`*正面`+270 / 主軸Z→`*右側面`+0)
  → 改良案 v2 = `view_orient.presentation_frame_v2`。**第2弾30点で独立検証した結果、下記のとおり否定された**

### ❗形状から正面を一意に決める試みは2世代とも失敗。正面選定は「候補提示+AI選択」問題である【フェーズ4第2弾・別の30部品で独立検証。詳細は 調査/phase4_scoreboard.md §9〜§16】
- **v2 は対照に勝てなかった**: 第2弾30点(判定できた26点)で **v2 53.8% = 対照 53.8%**、v1 38.5%。
  狙い撃ちした旋盤物でも **v2 3/6 対 対照 3/6** の引き分け(第1弾の 3/3 対 0/3 は導出元データの過学習だった)。
  60点合計では v2 62% > 対照 56% に見えるが、**差の+3点は全て第1弾(=v2の訓練データ)由来**
- **既定は「対照 = STEPの座標系そのまま(`*正面`・回転0)」で良い**。ルールを足すほど壊れる
- **★人間が選んだ正面は 50/50 = 100% が「SW標準6ビュー × 90度単位回転」の中にあった**(斜め視点はゼロ)。
  **候補集合は有限(最大24通り)で閉じている**。実際に使われたのは10通りで、
  **上位3候補で78%・上位6候補で90%**を被覆する(順位: `*正面`+0 56% → `*右側面`+0 72% → `*平面`+0 78%)
- **形状クラスで順位が変わる**: plate/holed_block は `*正面`+0 が65〜67%だが、
  **旋盤物だけ `*正面`+0 が33%しかない**(2位 `*正面`+270、3位 `*右側面`+0)。
  旋盤物の「主軸に直交するどちらから見るか」は**L/D でも同軸径本数でも分離できない**
  (5-07 L/D=0.07 と 3-05 L/D=0.12 で人間の選択が正反対)
- **推奨運用**: 事前分布順に上位6候補を投影 → PNG化 → **AIオペレータが目視で1つ選ぶ**。
  ゲート①②は選んだ向きに対して従来どおり決定論で効くので品質保証は崩れない。
  候補が拮抗したら人間へ質問票。**ルールを3つ回しても68%止まり**で、上位3候補(78%)に負ける
- **ギア・ねじ部品の事前隔離は成功**(選定時に走行ギア/ターンギア/ﾗｯｸ×2を隔離)。
  判定は品名より**図面文字(`モジュール` `歯数` `圧力角` `歯車`)が確実**。
  ❗**荏原の品名は半角カタカナ混じり**(`ﾗｯｸ3-2000` `ｶﾊﾞｰ`)で全角キーワードだけでは素通りする
- **❗`IView.Angle` は radian・正=反時計回り(CCW)**。`90` を代入すると90rad→2.0354radに折り返る(例外なし)。
  `ModelToViewTransform` は追随するので代入後に読み戻して検証できる。**回転は `GetOutline` を測る前**に入れる
- SW標準6ビューの軸マップ(実測): 正面(+X,+Y,+Z) / 背面(-X,+Y,-Z) / 左側面(+Z,+Y,-X) /
  右側面(-Z,+Y,+X) / 平面(+X,-Z,+Y) / 底面(+X,+Z,-Y) ※(paper_x, paper_y, 紙面手前)
- STEP1部品あたり7〜19秒(投影4ビュー+DXF+照合)。30点×2条件=60回で**SWのクラッシュは0回**

### ❗照合ツール(測定器)の罠【フェーズ4・2026-08-10。全て実害あり】
- **「左下のビューが正面図」は外れる**: 左側面図があると最も左下は左側面図になる(25154-4-15 指針)。
  取り違えると**向きルールの正誤判定そのものが誤る**。第三角法の構造整合
  (上下に並ぶ隣は幅を共有/左右に並ぶ隣は高さを共有)をスコアにして決める(`compare_views.assign_roles`)
- **用紙倍率を「作図範囲の最大X幅/410」で推定してはいけない**: 図枠外に出る引出線で幅が820を超え
  倍率2と誤判定 → 尺度が半分になり**部品ビューを1つも同定できず判定不能**(4件で発生)。
  **図枠テンプレートが100/113以上一致したら倍率1で確定**(`compare_views.FRAME_MATCH_MIN`)
- **❗↑の対処では足りなかった【第2弾で判明・実害15件】**: 図枠が一致しない**非バケットA図面は幅推定に
  落ちたまま**で、倍率2/3/4/**18** と出鱈目に外れる(尺度セルの探索アンカーもその倍率でスケールされ連鎖する)。
  第2弾30点中12点+第1弾3点が「部品ビュー0個=判定不能」だった。
  → **幅からの推定は完全に捨てる**。`sf`(DXF1単位→実物mm)=`(尺度分母/分子)/用紙倍率` は
  **取り得る値が離散的**なので、部品ビューが0個のときだけ候補 `{1,1/2,2,1/3,3,1/4,4,…}` を走査し、
  **モデル実測から作ったSWビュー外形3つとの一致で検算**して選ぶ(`compare_views.SF_CANDIDATES`)。
  偽陽性防止に「部品ビューが8個超の候補は捨てる」(`SF_MAX_PART_VIEWS`。3-07で`sf=5`が52個拾った)。
  **照合不能 19点→10点、非バケットAの判定成立 6/21→15/21**。救済された12点は全て正解が `sf=1` だった
- **人間図面のねじは3Dモデルに無い円を持ち込む**(M5→谷径φ4.134、M4→φ3.242)。
  ゲート④の円照合・人間側被覆はねじ表現円を除去してからでないと機能しない(不合格の主因)
- **鏡像(det=-1)判定も円照合も、ビュー対応が正しいと確定してからでないと偽陽性を出す**。
  判定順序は **向き一致 → 鏡像 → 幾何**。実測で鏡像フラグは向きを直すと4件→1件に減った
- **ギア・ねじ部品は幾何ラスタ照合では原理的に検証できない**(歯形・ねじ山が簡略図示)。
  30点中5点(ギア3・ボールネジ1・シャフト1)が照合不能。事前に別ゲートへ振り分けること
- **❗`sys.stdout = io.TextIOWrapper(sys.stdout.buffer, ...)` をモジュール読み込み時に重ねてはいけない**:
  import する側が同じ包み方をしていると外側のラッパーがGCされ `__del__` が共通バッファを閉じる
  → 以後の print が `ValueError: I/O operation on closed file`。`sys.stdout.reconfigure(...)` は冪等で安全
- 人間図面の作図誤差 4例目/5例目: **25154-3-20 補強材の大円がφ20(モデル実測φ23・3mm差)**、
  **25154-6-10 押しボルト座の平面図の円がφ6(モデル実測φ7・1mm差)**。いずれも重ね合わせPNGで目視確認済み
- **❗逆に「3Dモデルの方が不足している」ケースが出た【第2弾・25154-5-07 エンドプレート】**:
  人間図面は中央穴まわりに **φ25の円**+側面図の2本線(=ザグリ)を描くが、モデルは **φ12.5の穴だけ**。
  外形φ80とボルト穴4本は完全一致なので描き間違いではない。
  → **「照合の正は3Dモデル実測」はモデルが正しく作られている前提**であり、
  **ザグリ等の加工形状がモデルに入っていない場合は原則が破れる**。この種の差は人間確認へ回す

### 図枠の実態【フェーズ1・2026-08-09確立】
- **図枠は1種類ではない**: 用紙サイズ・倍率で**34バケット**に分かれる(「品名」ラベル座標がキー)。
  最多の**バケットA(A3実寸410×287・図枠(5,5)-(415,292)・80/190枚・全9軸に存在)**を
  標準出力テンプレートに採用(ディレクター裁定: 新規生成はA3統一、大物は尺度で対応)
- 成果物: `図枠/frame_template.dxf`(共通112エンティティ・AC1015・cp932。2026-08-10: 113→112、
  詳細は本節末尾の❗ブロック名の罠を参照)、
  `図枠/fields.json`(可変15フィールドの座標・書式マップ)、`engine/frame_extract.py`(subtract_frame)
- **材質は専用セルが無い**: 左上ノートに `材質　ＳＳ４００　ＰＬ６ \P 個数　１` の自由テキスト2行形式
  (購入品は メーカー/品名/型式/個数 の4行形式で同じ枠を使い回す)
- **重量セルは過去図面で未記入運用**(80枚に記入例ゼロ)→ 新運用として自動記入する(ユーザー仕様)
- STYLE名(GMM0xx)はファイル毎の自動採番 → スタイルは**名前でなくフォント実体**(font/width_factor)で比較
- ezdxfで保存する時は `doc.encoding` を明示設定しないとヘッダ変数と無関係にANSI_1252へ化ける
- 表記は全角文字(図番２５１５４－１－０７等)。MTEXTの\T/\W書式コードを保持すること

### ❗ブロック名BLOCK00xはファイルローカル(中身が別物)。INSERTの同一性判定は必ず展開内容で行う【2026-08-10・ユーザー発見の実害から確立】
- **事象**: 生成DXFの右上・図枠外(用紙(0,0)-(420,297)の外、実座標x=519〜639,y=813〜1077)に、
  部品と無関係な「ピニオン断面図の残骸」(LINE29+ARC2+LWPOLYLINE1=32エンティティ)が描画されていた。
  ユーザー目視で発覚(ACROVA GMM等で開くと右上遠方に余計な図形が見える)。
- **原因**: `図枠/frame_template.dxf` はテンプレート構築時、18ファイルの完全一致シグネチャ(=共通図枠)を
  抽出して作られたが、INSERTエンティティのシグネチャに**ブロック名を含めて**比較していた
  (`entity_sig`/`entity_signature` の INSERT分岐: `(t, layer, color, e.dxf.name, insert, xscale, yscale, rotation)`)。
  **ブロック名 `BLOCK00x` はDXF読み込み時にファイルごとへ自動採番され直り、同じ名前でも中身(ジオメトリ)が
  別物**になる(STYLE名 `GMM0xx` と同種の罠。過去に発見済みのSTYLE/LAYER名トラップと同じ穴に、
  今度はINSERT/BLOCK名で落ちた)。テンプレート構築の基準ファイル(25154-1-07_走行ギア.dxf)に
  たまたま挿入点(0,0)・スケール1・回転0で「BLOCK002」という名のピニオン断面図の残骸が混入しており、
  同じ挿入変換+同じ名前を持つ実ファイルが少数(18枚中/バケットA80枚中2枚のみ、いずれも「走行ギア」系)
  存在したために「完全一致=図枠の一部」と誤判定された。挿入点・スケール・回転がすべて恒等変換
  (0,0,1,1,0)のINSERTは、実際の描画位置がブロック定義側のローカル座標に完全にエンコードされるため、
  **挿入点だけでは複数の異なるブロックを区別できない**(このテンプレートには恒等変換のINSERTが5個あった)。
- **検出方法**: `INSERTの名前+挿入点」だけで比較するのではなく、`virtual_entities()` で展開した
  ワールド座標ジオメトリの内容そのもの(またはbboxの位置)をキーにバケットA実ファイル(80枚以上)と
  多数決を取る。ページ上の位置ごとに多数派の中身を求め、テンプレートの現状と不一致なら要修正。
  「多数派=不在(80枚中78枚がその位置にINSERTを持たない)」は、その要素が図枠ではなく
  混入した残骸である強いシグナル。手順は `調査/frame_block_audit.py`、判定結果は
  `調査/frame_block_audit.md` 参照。
- **修正**: BLOCK002のINSERTとブロック定義を`frame_template.dxf`から完全削除
  (共通エンティティ数113→112。他4個のINSERT(BLOCK004/005/006/007)は80/80で多数派と一致=正常、
  変更なし)。`engine/generate_drawing.py`・`調査/verify_generated.py`等の`frame_matched==113`
  ハードコードは112へ追従修正。生成済み5図面(TEST-002/003, テスト-004, AUTO-001/002)を
  新テンプレートで再生成し、展開込みスキャンで枠外エンティティ0件を確認。
- **教訓**: ブロック名(BLOCK00x)・スタイル名(GMM0xx)・レイヤ名は**いずれもファイルローカルな
  自動採番/流儀違いで中身が別物になりうる名前空間**。DXFエンティティの同一性判定は名前ではなく
  実体(ジオメトリ・内容・フォント実体等)で行うこと。特にINSERTは、挿入変換(insert/scale/rotation)
  が複数エンティティ間で同一になり得る(=挿入変換だけでは一意に識別できない)ため、
  **展開後(virtual_entities)のワールド座標ジオメトリで比較するのが唯一の確実な方法**。

### SW図面API(フェーズ2・2026-08-09実測。詳細は 調査/phase2_pipeline_report.md)
- **❗`IView.Position` にtuple/listを代入すると例外なしで壊れる**(xが捨てられyにx×1000が入る)。
  `VARIANT(VT_ARRAY|VT_R8,[x,y])` 必須。**代入後は必ず読み戻して検証**
- **❗ビュー尺度が勝手に2:1になる**: シート1:1でも `CreateDrawViewFromModelView3` のビューは
  `ScaleDecimal=2.0`、しかも `UseSheetScale` はTrueと嘘をつく。`v.ScaleDecimal=1.0` を明示代入。
  放置すると**寸法値と作図長が2倍ずれる最悪の事故**
- **★`IView.ModelToViewTransform.ArrayData` は列優先 `[r0..r8, tx,ty,tz, s, ...]`**、
  `sheet_x = s*(r0*X+r3*Y+r6*Z)+tx*1000` が誤差0.000000mm。SW実測3D座標→DXF座標が直変換でき、
  ゲート①も同経路で閉じる
- 図面テンプレ=`GetUserPreferenceStringValue(10)`。`ISheet.GetSize` は `GetSize(0.0,0.0)` で呼ぶ
- 接線エッジは `SetDisplayTangentEdges2(1)` 一択(PHANTOM線種になる。2はContinuousに混ざり区別不能)
- 隠れ線はHIDDEN線種で出る(レイヤは全て`0`なのでレイヤでは区別不可。振り分けはezdxf側の仕事)
- 中心マークは `SW_CENTERMARKSYMBOL_*` INSERT(中身はCONTINUOUSの直線8本)。bbox計算時は除外
- `GetOutline` はジオメトリ+11.88mm/片側。等角投影では実範囲と一致しない→実範囲はTransformから
- SPLINE/ELLIPSEが出る(円筒×平面の交線等)→DXF読み側は平坦化必須
- **材質未設定モデルは密度1000kg/m³(水)で質量が出る** → 重量自動算出は材質設定後に行うこと
- 出力DXFは AC1015 / $INSUNITS=4(mm) / 用紙左下原点 / 実寸 → 図枠と同座標系で単純合成可
- matplotlibの日本語は `MS Gothic` 全滅・`Meiryo`/`Yu Gothic` を使う

### 合成(フェーズ2.5)の知見【2026-08-09】
- `engine/compose_drawing.py` の compose() で「図枠+SWビュー+表題欄記入」の合成が成立
  (図枠112/112保持・2026-08-10: 113→112・フィールドアンカー誤差0.0mm・ビュー/図枠領域の重なりゼロ)
- **ezdxf.addons.drawing の汎用レンダラーは日本語txt.shxをtofu(□)にする**(rcParams無関係)。
  PNG化は手描き方式(プリミティブ+ax.text/Meiryo)= compose_drawing.render_png を使う
- **手描きレンダラーの文字幅は近似**なので、表題欄の文字が重なって見えるのは見かけ。
  人間の実図面を同じレンダラーで描くと同じ重なり方になる(比較で切り分け済み)。
  **目視ゲートで「文字の重なり」を不合格理由にする前に、必ず実図面の同条件描画と比較する**。
  最終的な文字配置の確認は ACROVA GMM で開く人間検証に委ねる
- fields.json の align は2フィールドで実図面と不一致(材質ノート=middle-left、連番=bottom-center採用)。
  compose_drawing.py の ALIGN_OVERRIDE が正。日付は REV_日付_3 が製図日付を兼ねる
- ビュー間ギャップはA1の40mmのままではA3に収まらない → compose側で15mmに詰めて再レイアウト
- 人間図面の穴注記は `2-%%c6.6` のように **%%c(φ)のDXF特殊コード**を使う(フェーズ3で踏襲)

### 自社の寸法流儀(フェーズ3-A・80枚コーパス統計・2026-08-09確立)
詳細は `調査/dimension_style_analysis.md` §8、決定版パラメータは `図枠/dimstyle_spec.json`。要点:
- **直径寸法は文脈で使い分ける【2026-08-09ユーザー確定・裁定Q1の更新】**:
  **円形ビュー(円が見えるビュー)の外径=ネイティブDIAMETER型**(dimtype=3。円を斜めに貫く
  寸法線+両端矢印。人間図面 GMM006 と同方式)/ **断面・側面(輪郭)ビュー=線形+`dimpost='%%c<>'`**。
  どちらも DIMSTYLE の dimpost は `%%c<>`。半径はRADIUS型+`dimpost='R<>'`。
  実装は `engine/dim_engine.py` の `DIAMETER_STYLE_DEFAULT`(計画側は kind='diameter' +
  context='circular_view'/'profile_view' と書く)。裁定が変わってもこの1定数の差し替えで戻せる
- XDATAオーバーライドは使わず**1寸法=1専用DIMSTYLE**方式
- 基本値: dimtxt=4.0/dimasz=3.0/dimexo=1.0/dimexe=2.0/dimdec=2/dimclrd=dimclre=3(緑)/
  dimclrt=7/dimdli=8.0/矢印=_OPEN30/文字=txt.shx w=0.75
- 公差: はめあい系=ネイティブdimtol方式、対称=%%p。**はめあい記号(H7等)は使わない文化**
- 穴注記は**φ(%%c)表記・全角が既定【2026-08-10ユーザー裁定で変更。キリは依頼時オプション】**:
  `２－%%c８　ザグリ%%c１１深さ７\PＰＣＤ６０` 形式。根拠: 盲検で荏原25154の人間図面10部品が
  キリ0件・全てφ表記(2026-08-09のキリ裁定は15015系1枚が根拠で、工番により流儀が違うと判明)。
  `dim_engine.HOLE_NOTE_DEFAULT` で切替可能を維持。タップは `Ｍ１０深さ２０`。
  ねじ呼び径ちょうどの下穴は人間図面6/6がМ表記(意味解釈層の実装課題)。
  複合注記は\P改行。実装は `engine/dim_engine.py` の `HOLE_NOTE_DEFAULT` + `build_hole_note_pattern()`
  (計画側は構造化 `spec` を書く)。φ表記・半角へ戻す場合もこの1定数の差し替えで済む
- 寸法線オフセット: 輪郭から16mm、2段目以降+8mm刻み
- ezdxf実装の罠: **dimdsep既定がカンマ**(46=ピリオドを明示)/**_OPEN30はARROWS.create_block()で明示生成**/
  公差ゼロ側はGMMは「0」表示(dimtzinでは再現不可→text側で整形。ディレクター裁定)
- 裁定(調査/ディレクター裁定_フェーズ3A質問票.md): 公差は作図計画の明示指定のみ/
  タップ記号JISB0205は初期実装・仕上げ記号は第2弾/表記は文脈ごとコーパス最頻値に統一

### 寸法記入エンジン(フェーズ3-B・2026-08-09実測)
- `engine/dim_engine.py` + `engine/plan_schema.md`(作図計画JSON v1.0)。ゲート①内蔵
  (期待値/実測/実ジオメトリの3点照合、0.01mm超で停止。反証テスト3ケースで停止動作を確認済み)
- **❗`dimfit`(287)はezdxfがAC1015に書き出さない**(保存時に`dimatfit`+`dimtmove`へ自動変換)。
  GMM実ファイルは逆に`dimfit`のみ持つ。読み戻し検証は`dimatfit`/`dimtmove`側で行う
- **❗ビュー分類の前に必ず`subtract_frame()`**(図枠外枠がisoビュー領域と交差し輪郭が図枠全体に化ける)
- `Dimension.has_xdata`はメソッド(`if e.has_xdata:`は常にTrue)。`e.xdata is not None`で判定
- ezdxfの`add_linear_dim`はoverride無しならXDATA(DSTYLE)を書かない=「1寸法=1専用DIMSTYLE」が自然に実現
- 公差ゼロ側「0」整形は描画後にアノニマスブロック(*Dn)内MTEXTを正規表現置換(`\S 0.000^…`→`\S 0^…`)
- `render_png`のDIMENSION描画は`virtual_entities()`展開必須+%%c/%%p/\S公差の可読化が必要
- 合成4ビューのfront-right間は18.16mmしかない→front右側の垂直寸法はoffset 11mmに詰める(コーパスp25内)
- **❗人間の参考図面はジオメトリの正解ではない**: 15015-P3-012_013のホルダー断面で「２－８キリ」の穴が
  実測φ7で作図されていた(モデルはφ8)。人間図面は「どの寸法を入れるか」の正解であり、
  **ジオメトリ照合の基準にしてはいけない**(照合は3Dモデル実測が正)
- **ユーザー裁定(2026-08-09確定)**: ①直径寸法は文脈で使い分け —
  **円形ビューの外径=ネイティブDIAMETER型(斜め矢印)、断面・側面ビュー=線形+%%c**
  ②穴注記は**キリ表記を踏襲**(`２－８キリ　ザグリ%%c１１深さ７` 形式。加工方法の情報を残す)
  → **❗2026-08-10のユーザー裁定で②は φ(`%%c`)・全角 が既定に変わった**
  (`２－%%c８　ザグリ%%c１１深さ７`)。キリは `defaults.hole_note={"notation":"kiri"}` のオプション。
  ①は現行のまま。plan_schema/dim_engine の既定に反映済みであること

### フェーズ4先行(TEST-003 端子棒 + ゲート②v1)の知見【2026-08-09実測】

**ネイティブDIAMETER型・公差・注記の罠(全てezdxf側)**
- **❗ezdxfのDiameterDimensionはφを必ず自前で前置する**(`render/dim_radius.py` の
  `RadiusMeasurement.format_text` が `PREFIX='Ø'`→DXF出力時 `%%c` を付ける)。
  DIMSTYLEに `dimpost='%%c<>'` を入れると描画文字が **`%%c%%c75`(二重φ)** になる。
  対策: dimpost はコーパス流儀のまま `%%c<>` を保持し、**描画キャッシュ側の重複だけ除去**する
  (`fix_duplicate_diameter_prefix`)。DIMENSION.text は `<>` のままなのでCAD側の再生成は正しい
- **❗ezdxfは公差スタックの高さ係数を `\H{:.2f}x;` で丸める** → dimtfac=0.625 が `\H0.62x;` に。
  GMM実ファイルは `\H0.625x;`。描画後に整形(`fix_tolerance_height_factor`)すると
  人間図面の寸法文字と **文字列完全一致**する(`\A1;%%c30{\H0.625x;\S 0^ -0.021;}` を実証)
- **❗ネイティブDIAMETER型はsnap検証が使えない**(寸法線が任意角で円周を貫くため、測定点が
  実ジオメトリの特徴点にならない)。代わりに**「そのビューに中心一致・直径一致の実在円がある」
  ことの検証(`circle_check`)を必須**にした。ゲート①の強度は落ちない
- **❗全角の穴注記(キリ表記)は半角前提の正規表現に一切引っかからない**。注記を機械解釈する側は
  必ず全角ASCII(U+FF01-FF5E)+U+3000 を正規化してから解析する(『ＰＣＤ６０』の取りこぼしを実測)
- **❗PNGレンダラーの公差スタック平坦化は区切り必須**: `%%c30`+`0/-0.021` を素で連結すると
  **『φ300/-0.021』**と読め、目視ゲートの誤判定源になる(`_clean_mtext` で先頭に空白を入れる)

**人間図面はやはりジオメトリの正解ではない(2例目)**
- 端子棒のφ25は測定点を端面 `(x=0, ±12.5)` に取っているが、端面はC1面取り後で実測φ23。
  生成側は面取り開始点 `(x=-1, ±12.5)` で測る。**寸法の集合は人間図面が正解、測定点は実ジオメトリが正解**

**ゲート②(寸法完全性)v1 = `engine/gate2_completeness.py`**
- 設計: 生成DXFの実ジオメトリだけから **特徴台帳**(円の直径 / モデル軸X・Y・Zごとの位置ノード)を作り、
  (a)寸法 (b)穴注記 (c)位置ノードのグラフ連結=和差導出 (d)対称軸まわりの径/幅寸法
  (e)幾何導出 √((D/2)²-(W/2)²)(円筒×平面の交線=二面取りの見え掛かり) でカバーできるか判定する。
  計画JSONは**座標系の復元にしか使わない**(計画の自己申告を信用しない)
- 各ビューの `model_to_draw` を単位ベクトルで叩けば「どのモデル軸が図面x/yか」が判る。
  等角投影は軸平行にならない→自動的に判定対象外に落ちる(判定を諦める場所が決定論的に決まる)
- タップ注記は **JIS B 0205 下穴径表**で φ8.5(M10)等へ解決すると図中の下穴円をカバーできる
- **人間図面の寸法集合は必ず1本の冗長寸法を含む**(全長=分割寸法の和。ホルダー40=35+5、端子棒78=28+50)。
  冗長は**不合格理由にせず警告**にする。実際 TEST-002/003 とも「未指定0件・冗長1件」で合格
- **❗反証テストは「1本消し」だけでは足りない**: 冗長性に吸収されて合格のままになる寸法がある。
  **冗長な組をまとめて消すケースを必ず併走**させること(TEST-002は7本中3本、TEST-003は9本中5本が
  1本消しでは検出されないが、組で消すと全て検出できた)
- v1の判定対象外(黙って無視せずレポートに列挙する): 面取り/テーパとその端点位置・面取り由来の円弧径 /
  スプライン・楕円(交差曲線) / 等角投影ビュー / ねじのピッチ・等級 / 表面性状・幾何公差・溶接記号 /
  **同値の別フィーチャー**(左右のφ25のように値も座標も同じものは区別できない)

**レイアウト**
- A3・4ビュー配置では front 上側の3段目(+32mm)が平面図の輪郭(隙間34.3mm)に迫る。
  **全長寸法は下側へ回す**のが安全(人間図面は2ビュー・2-up図枠なので上側4段でも収まっていた)

### ❗AIオペレータ層v1(人間図面を見ずにSTEPだけで作図)初号機の実測【2026-08-10・AUTO-001/002】
成果物: `生成図面/AUTO-001_クッション.dxf` `AUTO-002_ベアリングカラー.dxf` /
`解釈レポート/AUTO-001_クッション.md` `AUTO-002_ベアリングカラー.md` /
一式のスクリプト・計測・候補PNG・採点=`調査/phase5_ai_operator/`

- **確定戦略(上位6候補を投影→PNG→AIオペレータが目視選択)は 2/2 で人間と一致**。
  1-18 クッション(holed_block)=`*正面`+0 / 3-05 ベアリングカラー(lathe)=`*正面`+270。
  人間図面も両方**AIと同じ2ビュー・同じ左右配置**だった。
  3-05 は CLAUDE.md が「L/Dでも同軸径本数でも分離できない」とした難所だが、
  **「旋削品は軸線を紙面水平」というJIS B 0001の加工姿勢原則で目視選択すれば当たった**
- **寸法セット**: 1-18 = 一致3/不足3/過剰0、3-05 = 一致5/不足1/過剰0。**過剰は両部品とも0**。
  ゲート①②③は両方1発合格(最大差0.000000mm・未指定0件)
- **❗❗実験の汚染: 教師データの答えが CLAUDE.md と engine/view_orient.py に書き込まれていた**。
  `view_orient.py` の docstring に「1-18 クッション…**人間は穴軸を紙面法線にする**」、
  CLAUDE.md 知見に「v1が対照に勝った2件は**旋盤物(3-05)**」(=v1の`*正面`+270が正解の意)。
  **どちらも必読指定の文書**なので、向き2/2は「盲検」ではない。
  → **今後の盲検には、部品名を含む実測メモを除いた『クリーンルーム版プロンプト』
  (=確定戦略・JIS原則・流儀パラメータだけ)を用意すること**。
  恒久対策として **知見文書には「部品Xの人間の選択はY」を書かない**(統計と原則だけ書く)
- **❗人間図面の寸法は「対称軸上のフィーチャーにも位置寸法を明示する」**: 1-18 の穴は25□の中心だが、
  人間は `12.5 + 12.5`(下辺→穴中心線→上辺)を入れている。**ゲート②の対称軸ルール(判定モデル(d))は
  これを自動で合格にしてしまう** = 幾何が一意でも「製図としての明示性」を見ていない。
  → ゲート②に「対称軸上のフィーチャーは対称記号か位置寸法のどちらかを必須」を足すべき
- **❗人間は穴注記の`深さ`を輪郭ビューに実寸法で再掲する**(1-18 の `12` = ザグリ深さ)。
  JIS的には重複だが自社流儀。**穴注記に深さを含む場合、深さが見えるビューにも寸法を再掲する**
- **冗長寸法の4例目**: 3-05 は `2 + 2.75 = 4.75`(全長)。段付き旋盤物は
  「各段の長さ + 全長」が既定と考えてよい(1本だけ冗長になるがゲート②は警告どまり)
- **❗穴注記の表記(キリ/φ)はコーパス内で部品ごとに揺れる**: 1-18 の人間図面は
  `%%c５．５ザグリ%%c９．５深さ１２`(**φ表記・キリなし・区切り空白なし**)。
  2026-08-09裁定「キリ表記が既定」の反例。切替は `dim_engine.HOLE_NOTE_DEFAULT` の1定数で済む
- **❗「材質形状」は仕上がり形状ではなく調達する素材**: 3-05(仕上がり最大径φ40)の人間記載は
  **`Ｓ－Ｈ%%c４５×ｔ１２`**(削り代込みの素材)。**3Dからは原理的に決まらない**。
  材質・個数も同様(1-18=ウレタンゴム/2個。AIはMCナイロン/1個と外した)。
  → **材質・材質形状・個数は依頼JSONの必須入力**とし、AIに推定させない運用が正しい
- **人間図面の作図誤差3例目を独立に再現**: 3-05 内径の CIRCLE実体φ30.1(寸法文字`%%c30`・
  モデル実測φ30.0000)。ゲート④が落ちた唯一の理由がこれ

**新しい罠(全て実測)**
- **❗STEPインポート体では `IEdge.GetCurveParams3` が例外**(端点が取れない)。
  円エッジの中心・軸・径は `ICurve.CircleParams` で取れるので穴台帳はこちらで作る。
  直線エッジの端点が要るなら別経路が必要(bboxと面のGetBoxで代用できる)
- **❗尺度は 1.0 しか使えない**(→ **2026-08-10 修正済み。下記「尺度・レイアウト」節参照**):
  `dim_engine.build_view_transforms` は図面座標を `scale` 倍するのに、ゲート①は図面座標の実測を
  **モデルmmの期待値**と直接比較していた(`dimlfac` で補正していない)ため scale≠1 で全滅した
- **❗`compose_drawing.VIEW_GAP_MM=15` < `first_offset_mm=16`**(→ **2026-08-10 修正済み**):
  4ビュー合成では **front の上側・右側の1段目の寸法線が隣のビュー領域に食い込んで**いた。
  当時の回避策は「寸法を外周側(below/left)へ回す」「offset を人手で詰める」(TEST-002 の
  `offset_mm: 11` はその名残)。**使わないビューを出さない選択肢**も無かった
- **❗ezdxf の Vec3 はスライス不可**(`e.dxf.defpoint[:2]` が `TypeError: slicing not supported`)。
  `.x` `.y` で読む

### 尺度・ビュー間隔・ビュー数選択の3欠陥を修正【2026-08-10・engine貫通。回帰は既存5図面で数値一致を確認】

対象: `engine/compose_drawing.py` / `dim_engine.py` / `gate2_completeness.py` /
`generate_drawing.py` / `plan_schema.md`。試験は
`調査/run_scale_test.py`(1:2)・`run_layout_interference_test.py`(干渉・反証つき)・
`run_twoview_test.py`(2ビュー)・`run_regression_all.py`(既存5図面の回帰スナップショット)。

- **①尺度: 寸法値は尺度に関係なく『モデル実寸』を表示する**(自社流儀。人間も1:2図面に実寸を書く)。
  実装は **DIMSTYLE `dimlfac = 1/scale`**。ezdxfは描画時に `measurement × dimlfac` を文字にする
  (`render/dim_base.py` 実測)。**ゲート①②・独立検証はすべて『モデル実寸空間』で照合する**
  (`dim_engine.measure_model_value(dim, scale)` が図面実測を 1/scale して戻す)。
  ❗**実ジオメトリとの突き合わせ(実在円の探索)だけは図面座標=実寸×scale で探す**。
  ここを混ぜると `circle_check` / `cross_check` / `anchor_check` が偽陰性になる。
  ❗**ゲート②も単位が混在していた**: 位置ノードは `to_model_coords` で実寸へ戻るのに
  **円の直径だけ図面座標のまま**だった(scale≠1 で「φ40の円をφ40の寸法がカバーしない」と誤判定)。
  実測: ホルダーを1:2生成 → 作図は全て1/2・寸法文字は 40/35/5/φ33/φ26/φ75/60(実寸)・
  ゲート①②③+独立検証すべて合格・尺度欄「１：２」
- **②ビュー間隔は『寸法の予約帯』から計画駆動で決める**(固定値の握り合わせをやめた)。
  `予約帯 = そのビュー・その辺の最大寸法線オフセット + 5.5mm`、
  「隣り合うビューの隙間 ≧ 双方の予約帯の和」を満たす最小の間隔を採る(下限は従来の15mm)。
  ❗**帯の実測値**: 寸法文字が寸法線の外に出るケースで `dimgap 0.5 + 文字の実描画高さ 4.5552`
  (=`dimtxt 4.0 × 1.1388`。**ezdxfのフォント実測でchar_heightそのままではない**)= 5.0552mm。
  文字が内側に収まるケースは `dimexe 2.0`。**4.5mm(=dimtxt+dimgap)で見積もると0.56mm足りず干渉が残る**。
  反証つき実証: 同じ計画で `layout.dim_reserve=false`(従来固定15mm)=干渉1件 →
  `true`(新既定)=干渉0件。判定は生成DXFの DIMENSION を `virtual_entities()` 展開した
  bbox と他ビューの実ジオメトリ bbox の交差で機械確認
- **③使用ビュー集合を計画JSONで指定できる**(`layout.views`。省略=従来4ビューで後方互換)。
  compose/dim_engine/gate2/generate_drawing を貫通。使わないビューのエンティティは取り込まず、
  残ビューは紙面中央へバランス配置(第三角の相対整列は保持)。
  実測: AUTO-002 を front+right の2ビューで再生成し全ゲート合格(人間図面と同じ2ビュー構成)
- **❗計画JSONの `space:"view"`(図面絶対座標)はレイアウトが変わると壊れる**: 尺度・ビュー集合・
  予約帯のどれを変えても図面座標が動くため、`snap`/`anchor_check` が落ちる。
  **測定点・引出線は原則 `space:"model"` で書く**(TEST-002 の穴注記引出線が唯一の view 指定で、
  1:2版を作る際に再計算が必要になった)
- **❗レイアウトの正は計画JSON**(`source.scale` + `layout`)。compose と dim_engine が
  **違う引数でレイアウトを計算したら全寸法がゲート①で落ちる**ので、
  4者とも `dim_engine.plan_layout(plan)` から同じ3点セット(尺度・ビュー集合・予約帯)を読む。
  依頼JSONの `尺度` が計画と食い違ったら generate_drawing がエラーで止まる
- **回帰の作法**: `調査/run_regression_all.py --out <snap.json>` で既存5図面
  (TEST-002/003・テスト-004・AUTO-001/002)を再生成し、ゲート①全行・ゲート②反証一式・
  ビューbbox・独立検証を1つのJSONへ正規化する。**修正前後で差分0件**を確認した
  (DXF実体もタイムスタンプとFINGERPRINTGUID以外バイト一致)。
  ❗**generate_drawing.py は実行のたび台帳へ1行追記する** → 回帰試験では `--no-ledger` を使う
- **❗生成図面/のDXFはユーザーがeDrawings等で開いているとロックされ `PermissionError` で上書きできない**。
  回帰試験は `--gen-out-dir` で別フォルダへ出す(納品物を壊さない)

### 改善サイクルA: ゲート②のPCD/多角形対応・独立検証のPCD誤読・図枠衝突検出・候補集合【2026-08-10】

盲検10部品(`調査/blind_test_report.md`)のボトルネック4件を修正。再判定ハーネスは
`調査/run_blind_regate.py`(生成済みDXFにゲート②/独立検証/レイアウト検証を掛け直す。SW不要)、
反証は `調査/run_cycleA_falsification.py`(10/10合格)、回帰は `調査/run_regression_all.py`(差分0件)。

- **ゲート②に「PCD穴群」を入れた**。穴注記の `ＰＣＤ<値>` を位置カバレッジの根拠として採用するが、
  **注記の自己申告をそのまま信じない**: (a)PCD値が実ジオメトリの穴中心円と0.01mm以内で一致
  (b)注記の個数と実個数が一致 (c)隣接角が360/nと0.05度以内で一致(円周等分)
  の**3点を実ジオメトリで検算できた場合だけ**採用する(`find_pcd_groups`)。
  位相(基準角)は判定対象外として `out_of_scope` に明記する
- **❗PCD採用の最小個数は3個**(`PCD_MIN_COUNT`)。2個(180度対向)も数学的にはPCDで決まるが、
  採用すると **TEST-002の反証テストの検出が 4/7 → 3/7 に落ちた**(`P60_hole_pitch` を消しても
  合格のままになる)。**「通せる」ことと「検出力を保つ」ことは別**で、後者を優先した
- **正多角形(六角座)を1特徴として扱う**(`find_regular_polygons`)。線分の閉ループから
  頂点等距離+等角を検算して正n角形を同定し、**二面幅または対角の寸法1本**で全頂点を決まりとする。
  未指定なら6頂点をバラバラに並べず**「正6角形(二面幅24)を決める寸法が無い」1件**で報告する
- **ねじ注記は下穴径と呼び径の両方をカバー径にする**(`parse_hole_note`)。
  荏原の設計者は3Dに呼び径ちょうどの穴を立てる(盲検6/6)ので下穴径だけでは取りこぼす
- **面取り由来の円径導出は `d0 ± 2×leg` の両方向**にする(穴の口元C1で φ72→φ74 と**増える**側がある)
- **❗独立検証が参考寸法を数値照合していた**(盲検で誤不合格4件)。`text_override` が
  `(ＰＣＤ３３３)` のように**括弧で囲まれていたら数値照合をしない**(`is_reference_text`)。
  代わりに**「計画が指定した文字がそのまま描かれているか」を必ず検査する**(`text_override_applied`)
  ので検証の穴にはならない。`parse_dim_text_value` は括弧を剥がす実装なので**単独で使ってはいけない**
- **❗`layout.collisions` は寸法『文字』しか見ておらず、図枠エンティティも見ていなかった**。
  盲検5-08で寸法が表題欄に載ったまま全ゲート合格で納品された。判定を
  **`virtual_entities()` 展開の1プリミティブごとの矩形 × (表題欄/左上ノート/図枠エンティティ/図枠外)**
  に変え(`check_frame_collisions`)、1件でもあれば `generate_drawing` が **不合格(`layout_ok`)** にする。
  ❗**寸法1本の全体bboxで図枠と当ててはいけない**(細い補助線しか通らない空白まで衝突扱いになる)。
  ❗ページ全体を囲む図枠エンティティ(外形LWPOLYLINE)はbbox交差が無意味なので個別照合から外し、
  `outside_frame`(FRAME_RECTへの内包)判定に委ねる
- **候補集合を6→8に拡張し、事前分布を60点(フェーズ4の50点+盲検10点)で引き直した**
  (`調査/recompute_front_prior.py` / `調査/front_prior_60.json`)。
  全体順位: `*正面`+0 53.3% → `*右側面`+0 68.3% → `*平面`+0 75.0% → `*平面`+270 81.7% →
  `*正面`+270 85.0% → `*平面`+90 88.3% → `*左側面`+0 91.7% → `*平面`+180(+180の穴埋め)。
  被覆率は **旧6候補 85.0%(盲検7/10) → 新8候補 95.0%(盲検9/10)**
- **❗「旋盤物は例外なく主軸を紙面水平」は過大な一般化**(盲検の教訓B)。60点中13点の旋盤物で
  人間が選んだ正面における主軸の見え方は **水平 8/13(62%) / 円形(主軸=紙面法線)4/13(31%) /
  垂直 1/13(8%)**。**円形ビューを1位から外すのは正しいが、候補から外すのは誤り**。
  実装は実測比 8:4:1 の比率マージ(`app/candidates.py` の `_ratio_merge`)で、
  円形ビューは必ず3番目に現れる。旋盤物の候補順は `view_orient.classify` の `main_axis` に依存する
  → `candidates_for(shape_class, main_axis)`
- **穴注記の既定を φ(`%%c`)・全角へ変更【2026-08-10 ユーザー裁定】**:
  `HOLE_NOTE_DEFAULT = {"notation":"phi","width":"zenkaku","separator":" "}`。
  出力例 `２－%%c８　ザグリ%%c１１深さ７`。**キリ表記はオプションとして温存**
  (計画の `defaults.hole_note` に `{"notation":"kiri"}`)。既存4計画は全て `defaults` で
  キリを明示していたため**回帰は完全に無変化**だった
- **再判定の実測(盲検10点・生成済みDXFを再検査)**: ゲート② **3/10 → 4/10**(4-07が救済。
  未指定件数は 1-04 31→20 / 2-06 69→44 / 3-04 42→41 / 5-04 132→123 / 6-02 30→10)、
  独立検証 **6/10 → 10/10**(誤不合格4件を解消)、レイアウト検証は新設で **6/10**
  (5-08の実害+2-06/3-04/6-02の図枠外/表題欄侵入を新規検出)。総合は 3/10 のまま
  (5-08が図枠衝突で不合格化し、4-07が合格化して入れ替わった)
- **❗残る不合格はほぼ全て「エンジンの限界」ではなく「AIオペレータの計画の問題」**だった:
  基準面からのチェーンを作らず孤立した寸法対を並べたため位置グラフが非連結(1-04・3-04)、
  PCDの無い穴群の位置を指定していない(6-02)、注記に等配でない穴群のPCDを書いた(5-04)、
  テーパねじ・傾斜穴・角度配置の耳(3-09・2-06)。**次の律速は計画側の作法**

### 修理パス(不合格7件の再挑戦)と計画作法ルールの実測【2026-08-10・詳細は 調査/repair_pass_report.md】

盲検の失敗分類から `app/prompts/plan_prompt.md` に**「計画の作法」5項目**を明文化し、
不合格7件を `workshop.py retry` で巻き戻して計画を書き直した(**盲検ではなく修理パス**。
計画は3D実測とゲートフィードバックのみから作り、人間図面は開いていない)。

- **納品率 3/10 → 4/10**(5-08を回収)。レイアウト検証は **6/10 → 10/10・図枠衝突20件→0件**。
  7件の未指定ノード合計 316 → 250(改善A) → **204**。
- **❗「計画の作法」を明文化した効果は決定的**: 基準面チェーン(作法1)を適用した3部品で、
  **狙った位置ノード41件が41件とも解決**した(3-04は「軸受穴中心線→端面71/上面45」
  「中心平面→ボルト穴17」の**3本**でX軸18ノードが一括解決)。
  盲検の主因「孤立した対寸法の羅列」は**プロンプトで完全に潰せる**。
- **❗図枠ゾーン回避(作法4)には縦方向の予算式が要る**: ビュー群は**幾何だけ**が
  y=30〜292 の中央に置かれる(外周の寸法帯を含めない)ので、上下の余白は必ず等分される。
  `2×max(下側予約帯, 上側予約帯) + ビュー間隔 ≤ 262 − 幾何合計高さ`。
  **寸法線位置から外側へ最大10.3mm** 描画実体が出る(垂直寸法は測定点の上下±3mm)。
  段数が足りなければ **1本の寸法線に直列で複数寸法を並べる**(重なりが無ければ正当。作法1と同じ形)。
- **❗φ222やφ420の円形ビューはA3の上下余白が20〜26mmしか残らず、段積みが物理的に不可能**。
  尺度は「作図が入るか」でなく **「寸法帯が入るか」**で選ぶ必要がある。
- **❗引出線のランディングはビューごとに『どのモデル軸が図面x/yか』を確かめてから決める**。
  5-08は平面図でモデルX方向へ折っており、図面上は垂直になって図枠上端を突き抜けていた。
- **❗М表記化はPCD穴群の検算を壊さない**。`parse_hole_note` はねじ注記から呼び径と下穴径の両方を
  カバー径にし PCD も別に拾うため、`find_pcd_groups` の実ジオメトリ検算は従来どおり通る(5部品で確認)。
  М判定は**「呼び径と±0.1mm一致 かつ 3個以上が円周等配」**に限定し、根拠を計画コメントに残す運用にした。
- **❗残る不合格6件はすべて「ゲート①合格・独立検証合格・レイアウト検証合格、ゲート②のみ不合格」**。
  = 値も書式も図枠も正しく、**寸法完全性の判定モデルが対応していない形状だけが残った**。
  クラスは5つ: ①円周上の群(クラスタ)配置穴群(5-04。実測隣接角[12,18,12,78]×3で円周等分でない)
  ②傾斜穴(投影が楕円SPLINEになりその端点が到達不能ノードになる。3-09・2-06)
  ③テーパねじRc/G/PT(`parse_hole_note`が`[ＭM][0-9]`しか見ない。3-04)
  ④回転した正多角形の二面幅(軸平行寸法では測れず、斜め寸法は`oblique_dimension`で捨てられる。1-04)
  ⑤円弧×円弧・片側弦の交点(6-02)。
  **投入コストあたりの納品増は ④→⑤→③→②→① の順**(④+⑤だけで 4/10 → 6/10 の見込み)。
- **❗`gate2` の対称判定は多角形頂点で壊れる**: 1-04のX軸は六角頂点を除けば完全に0対称で、
  除外できていれば対寸法(150/140/100/50)だけで解けていた。
  「多角形を1特徴として別扱いする」のに「対称判定からは除外しない」のが穴。
- **❗検証が強化されたら既納品を再判定する経路が要る**(`workshop.py retry --force` を追加)。
  5-08は旧エンジンで合格・納品済みだったが、改善Aの図枠衝突検出では不合格の状態だった。
  巻き戻し時は**納品箱の複製も消す**(検証を通っていない旧DXFを残さない)。
  併せて `不合格理由.json` に `layout_ok`/`frame_collisions` を記録するようにした
  (従来は図枠衝突だけで落ちると「不合格(理由詳細なし)」としか出なかった)。
- **❗`retry` は台帳を更新しない**ため、巻き戻した依頼の台帳行が「不合格」+消えたパスのまま残る。
  加えて盲検時代の重複行(1図番3〜4行)が `依頼ID=` マーカー無しで残っている。**正はマーカー付きの行**。
- **向きの再選択は実施していない**: 3件(2-06/5-04/3-09)の正解は blind_test_report §2.1 で開示済みで、
  今選び直しても `orientation_prompt.md` の検証にならない。かつ3件の不合格原因は向きに依存しない。
  **向き選定の実力測定は次回の新規盲検で行う**。

### 中心線エンジン(ユーザー最重要フィードバック「中心線が無く見にくい」への回答)【2026-08-10・人間図面144枚で流儀採取。詳細は 調査/centerline_style_analysis.md】

実装 `engine/centerline_gen.py`(既定ON。計画JSONの `defaults.centerline.enabled=false` で抑制)。
採取 `調査/analyze_centerline_style.py` / 反証 `調査/run_centerline_falsification.py` /
差分検算 `調査/verify_centerline_only_diff.py` / 並置PNG `調査/compare_centerline_png.py`。

- **線種・色は完全に決まっている**: 荏原の中心線は **DASHDOT 2290/2290 = 100%**、
  **色3(緑) 98.6%**。レイヤ名は `3`/`1`/`LEVEL003`/`255`/`0` とバラバラで**意味を持たない**
  (ファイルローカルな名前空間の3例目。ブロック名BLOCK00x・スタイル名GMM0xx と同じ罠)。
  ❗**`図枠/frame_template.dxf` には DASHDOT が人間図面と同一パターンで既に定義済み**で、
  生成DXFの `$LTSCALE=8.4` は人間図面(用紙倍率1.5)の `12.6=8.4×1.5` と一致する=**ピッチが同じに見える**
- **延長量は「比」ではなく「絶対量」**。用紙倍率1の83枚で
  **穴クロスの円外への延長=中央値5.00mm(最頻5.0・p25 4.50・p75 7.00)**、
  **ビュー貫通軸の輪郭外へのはみ出し=中央値4.56mm(p25 3.33・p75 7.67)**。
  作図空間(DXF単位)で見ると**10.0が最頻(49%)**で、倍率2/3/4の紙面値5.0/3.3/2.5はこれで説明が付く。
  円径への依存はほぼ無い(`延長/半径` は中央値1.67・p75 2.50とばらつく)。既定は両方 **5.0mm**
  (第1段の寸法線オフセット16mmの内側なので寸法と干渉しない)
- **❗PCD参照円を「幾何だけ」で判定してはいけない**: 「同心・等距離の穴が3個以上」を条件にすると
  **長方形配置の四隅4穴**(25154-1-27 走行フレーム踏板)で板全体を覆う巨大な偽PCD円が出た。
  正方形配置なら角度等分検査も通ってしまう。**人間の流儀は「注記が ＰＣＤ と書いたら参照円を描く」**
  → 実装は**穴注記のPCD値を根拠**にし、`gate2_completeness.find_pcd_groups` と同じ3点検算
  (個数一致/中心円径一致/円周等分)を実ジオメトリで通ったものだけ描く。
  結果は人間図面と完全一致(1-27・4-05=0本 / 3-02=2本 / 5-05=1本)
- **❗一直線に並ぶ穴は1本の中心線でつなぐ**(25154-1-27の実測)。穴ごとに十字を切ると細切れに見え、
  「図面として見にくい」という元の指摘に逆戻りする。統合で 4-05 は53→36本、1-27 は18→11本になり
  人間図面と同じ見た目になった
- **人間の被覆率は67.2%(同心円を1件に正規化した883件)**だが、**φ25以上の円は100%**。
  小径穴の3割は作図者の裁量。**生成側は全ての円形フィーチャーに縦横を付ける(超集合)**方針を採る
- **❗中心線は注記であって幾何ではない**。`dim_engine.classify_view_geometry` に線種フィルタ
  (`CENTERLINE_LINETYPES`)を入れ、ゲート①②の幾何照合・位置ノード・円フィーチャーから除外した。
  これが無いと、中心線を足した図面を `workshop retry` / `run_blind_regate` で再検査したときに
  **PCD参照円が「径寸法の要る円」に化けてゲート②が落ちる**
- **チェーン上の位置は「全ゲートの後・PNGの前」**。こうすると回帰差分が「中心線の追加のみ」に
  なることを機械確認できる(`verify_centerline_only_diff.py`。中心線以外の署名多重集合が完全一致・
  削除0を要求)。実測: 回帰3件+BLIND2の5件=8件すべて○
- **ゲート③の機械化第一歩** = `centerline_gen.check_centerlines`。判定は3本立て:
  (1)円形フィーチャーの縦横被覆と円外への延長量 (2)実ジオメトリから決まる設計が1本残らず
  実体として存在するか (3)図枠/表題欄/左上ノートへの侵入。
  ❗**(1)だけでは足りない**: 輪郭ビューの穴軸マーク・PCD参照円の欠落は被覆判定に映らず、
  反証の「任意の1本消し」が素通りした(2/10)。(2)を足して**42/42検出**になった
- **❗反証テストの出発点は中心線を1本も持たない図面でなければならない**。納品箱のDXFは
  中心線入りへ更新され得るので、そこを種にすると「消した1本が今回の設計に含まれない古い中心線」
  になり検出されないのが当たり前になる(実際にこの罠に落ちて8件の偽の未検出が出た)
- **❗SW投影の円は4分割ARCで出る**ので `CIRCLE` だけを見ると穴が1つも見つからない
  (25154-2-16 の modelspace の CIRCLE は図枠の連番マーク1個だけ)。ARC合計350度以上を円として拾う
- **❗納品箱のDXFはユーザーがeDrawingsで開いているとロックされ、`workshop retry --force` が
  `PermissionError` で落ちる**(納品箱の削除段で死ぬ)。CLAUDE.md既知の「生成図面/のロック」と同じ罠が
  納品箱でも起きる。再生成を頼む前に「該当DXFを閉じてください」と確認すること

### 様式エンジン第3弾「上品さの3層構造」(人間図面4部品レビュー27論点の実装)【2026-08-11】

仕様は `調査/図面品質メモ_指針レビュー_2026-08-11.md`(27論点+ユーザー裁定)。
実装 `engine/nominal_size.py`(新規)+ `engine/dim_engine.py` + `engine/gate2_completeness.py`、
プロンプト `app/prompts/plan_prompt.md`(作法12〜17)/ `orientation_prompt.md`(原則8)。
反証 `調査/run_style3_falsification.py`(17/17)、測定 `調査/measure_extension_collinear.py`、
ショーケース `調査/run_style3_showcase.py`、並置PNG `調査/compare_style3_png.py`。

- **❗❗論点7(補助線が輪郭線と溶ける)の仮説は2つとも反証された。真因は「補助線の長さ」だった**
  (`調査/measure_extension_collinear.py`・人間5枚 vs 生成5枚を同じ判定器で測定):
  - 仮説A「生成は輪郭と同一直線上に補助線を出すから溶ける」→ **人間 59/88=67.1% / 生成 46/63=73.0%**
    でほぼ同率。**人間も日常的にやっている**ので、それ単独では欠陥ではない。
  - 仮説B「すき間(dimexo)が足りない」→ 紙面上の dimexo は**コーパス927/927=100%が1.0mm**で
    生成も同値。しかも**描画実体を測ると付け根のすき間は dimexo と 0.0000mm 一致**していた
    (=dimexoは実DXFで正しく機能している)。**すき間を広げる自動回避は流儀から外れるので既定OFF**。
  - ★効いていた差は**長さ**: 人間 中央17.2 / p75 23.1 / p90 29.1 / 最大63.7mm に対し
    生成 中央39.8 / p75 68.3 / p90 114.2 / **最大211.4mm**。A3に211mmの緑の直線が輪郭の延長として
    走れば「図解の線」に見える。今泉さん指摘の実体はこれ。
  - 原因は構造的: 寸法線は**ビュー外接矩形**から offset だけ外に置かれるので
    `補助線長 =(測定点からその辺の輪郭までの距離)+ offset`。
    **測定点が載っている辺に寸法を置けば offset(16mm)=人間の中央値17mmで済む**。
    → エンジンの仕事は検出と警告、直すのは計画側(`placement.side`)。
  - ❗**測定器を用紙倍率で正規化しないと結論が逆になる**: 人間ファイルのDXF単位 dimexo
    4.0/3.0/1.5 は用紙倍率であって紙面量ではない。固定しきい値で測ると倍率4の図面が
    自動的に「溶けていない」と出る(CLAUDE.md『照合ツールの罠』と同型の事故)。
- **呼び値翻訳層 `engine/nominal_size.py`**(論点8・15。ユーザー裁定「事実に無い補完はしない」):
  **注記の径値だけ**を JIS呼び値表(φ1.0〜13.0=0.1刻み/φ13.5〜50=0.5刻み/φ51〜100=1.0刻み・
  許容窓±0.05mm)で翻訳する。**寸法値(ゲート①②の正)は一切触らない**。
  - 窓幅0.1mm は「φ7.04→φ7(差0.04)を通し、90°皿もみのφ13.44(最寄りφ13.5・差0.06)を通さない」
    という、レビューで実際に問題になった2値を分離する最小の窓として決めた。
  - 丸め先が自明でない値・φ100超は**丸めずに「呼び値未確定」**として解釈レポート/質問票へ回す
    (`report.nominal.pending` → `workshop._style3_section`)。φ219.1 は「呼びで語る範囲外」で拾う。
  - ❗**翻訳の正しさを保証するのは表ではなく検算**。`dim_engine` は呼び値表にも翻訳関数にも
    依存せず `|呼び値−実測| ≦ 窓` を自前で確かめる。表や関数を偽装した嘘の丸め(φ7.04→φ8)は
    **ゲート①不合格**になる(反証 N4)。表を偽装した場合は**丸めずに未確定へ倒れる**(N5)。
    許容窓は**計画から上書きさせない**(窓を広げれば翻訳も検算も通る抜け穴になるため)。
  - ❗**翻訳を入れたらゲート②の径カバー判定も直さないと必ず落ちる**: 注記φ7が実ジオメトリφ7.04を
    覆えなくなる。`gate2_completeness.NOTE_DIA_TOL = 0.05`(**注記の径だけ**呼び値窓で照合。
    寸法値は従来どおり `VALUE_TOL=0.01`)。
- **円形ビューの径は1本まで**(論点14)= `dim_engine.check_circular_view_diameters`。
  超過は様式警告(生成は止めない)。実測でBLIND-6-02(front 3本)とBLIND2-5-05(top 3本)を検出し、
  2本目以降を輪郭ビューへ移して**大円内のX字交差が消えた**(並置PNGで目視確認)。
- **注記は対象の近くに**(論点21)= `dim_engine.auto_place_hole_note`(`hole_notes[].auto_place`)。
  対象円の外周から10〜36mm・8方向(ビュー外向き優先)を近い順に試し、
  文字枠がビュー幾何/既配置文字枠/表題欄/左上ノートと衝突しない最初の位置を採る。
  置けなければ計画値へフォールバック+様式警告。`anchor_check` が無い計画でも
  **引出線の始点は動かさず**折れ点から先だけを置き直せる(半径0モード)。
  引出線長の目安10〜50mm(人間実測)を超えると様式警告。
- **様式警告は生成を止めない**(ゲート①②とは別の品質軸)。`report.style_warnings` →
  `result.json` → 解釈レポートの「様式(上品さの3層構造)の計測」節と「人間に確認が要る項目(質問票)」節。
- **ビューの紙面配置は計画では変えられない**(第三角法で front=左下 固定)。
  「顔をどれにするか」の制御点は**向き選択(`orientation_prompt.md`)だけ**である。
  よって論点20/25(フランジ=円形が顔 / ブラケット=なりが見える面が顔)は
  `layout.views` の並び順ではなく候補選択の規準として実装した(原則8)。
  `layout.view_grid` のような上書きは**JIS第三角法を壊す**ので実装していない。
- **`space:"view"` の計画座標は改訂のたびに壊れる**(既知の罠の再発。2-16のanchor_checkで実害)。
  尺度・ビュー集合を変えなければ**ビュー中心の平行移動だけ**なので
  `調査/style3_shift_view_space.py` で厳密に救済できる。**恒久対策は model 空間で書くこと**。
- ショーケース6枚(BLIND-6-02+BLIND2の5枚)を27論点レンズで再計画し**全ゲート合格を維持**。
  補助線長(紙面mm)は **中央39.8→29.0 / p75 68.3→41.5 / p90 114.2→65.3 / 最大211.4→149.75**
  (人間 17.2/23.1/29.1/63.7)。穴注記の引出線は **最長131.3→40.0mm**(17本中15本が自動配置)。
  **まだ人間の1.7倍**で、残差は「1本の寸法線に載る段数」と「向き選択」に起因する。

### 様式エンジン第2弾: 直列連記・左右振り分け・角度位相(ユーザー指摘「寸法の取り方が機械的で見にくい」の残り半分)【2026-08-11】

実装 `engine/dim_engine.py`(`check_chain_alignment` / `measure_angle_arc` / `check_angle_arc`)+
`engine/compose_drawing.py`(`chain_key` / `resolve_dim_offsets`)+ `engine/gate2_completeness.py`(様式寸法)。
反証 `調査/run_style_falsification.py`(10/10)、測定 `調査/measure_style_placement.py`、
並置PNG `調査/compare_style_png.py`(出力 `調査/style/compare/`)。

- **直列連記は `placement.chain_group` の1行で書ける**。同じ名前のメンバーは
  **グループ内の最大オフセットに正規化**されて1本の寸法線に並ぶ。
  ❗**オフセットの決定は `compose_drawing.resolve_dim_offsets` の1実装に集約すること**。
  レイアウト(予約帯)と作図で別々に計算していた従来構造のままグループ正規化を足すと、
  ビュー間隔と実際の寸法線位置がずれる(CLAUDE.md既知の「compose と dim_engine が
  違う引数でレイアウトを計算したら全寸法が落ちる」罠の再来になる)
- **整列の検証は実DXFの defpoint から行う**(計画の自己申告はメンバーシップにしか使わない)。
  同一直線・区間の非重複・端点共有・方向一致・side一致の5点で、破れたらゲート①不合格。
  ❗**「同じ offset を書けば勝手に並ぶ」ので、検証が無いと退行に気付けない**。
  反証はオフセット決定関数を旧実装へ差し替える**実装退行の注入**で取る(S2)
- **❗❗ezdxfの角度寸法は「優角(360−θ)側」に円弧を描いていた【重大・実害あり】**:
  `add_angular_dim_2l(base, line1, line2)` は
  **start_angle = line2 の向き / end_angle = line1 の向き**としてCCWに円弧を描く
  (`ezdxf/render/dim_curved.py` の `AngularDimension`。1.4.4 で実験確認)。
  旧実装は `line1=(v,p1), line2=(v,p2)` と渡していたため、
  **値と寸法文字は正しいのに円弧だけが反対回り**という図面を量産していた。
  実害: 25154-3-09 が「30度」の表示で**約310度の円弧**を持ったまま納品されていた。
  ゲート①も独立検証も**defpointからの再計算しか見ていなかったので素通り**した。
  → 対策は3点セット: (a)line1/line2を入れ替える (b)`measure_angle_deg` が
  **描かれた円弧**を返すようにする (c)`check_angle_arc` が
  **アノニマスブロック内のARC実体**を読み「円弧の中点が測定セクタ内か」
  「円弧長が測定角±矢印逃げに収まるか」を検査する。
  **計画JSONの書き方と値は不変**(p1→p2 のCCW)なので既存計画の修正は不要
  (3-09は再生成で30度の円弧に修正済み)。
  ❗**教訓: 「値が合っている」ことは「正しく描かれている」ことの証明ではない。
  作図系の検証は必ず『描画実体』を測る**(中心線ゲートで得た教訓と同じ形)
- **`purpose:"style"`(様式寸法)を導入**。ゲート②が判定対象外にしている情報
  (等配穴群の位相・面取り角)を示す寸法は、そのままでは必ず「宙に浮いた寸法」警告になり、
  **人間が実際にやっている作図を計画側が避ける動機**になっていた。
  `purpose:"style"` は警告から外すだけで、**カバレッジの根拠には一切使わない**
  (反証S8: 同じDXFで `purpose` 有無を比べ、`unspecified` は完全一致・`floating` だけが消える)
- **❗様式の効果は空間クラスタ方式の測定器では見えない**: `調査/style_pattern_analysis.py` は
  ビューをgap45mmでクラスタするため、2ビュー図面では**正面図と右側面図が1クラスタに融合**し、
  「正面図の左に置いた径」と「右側に置いた径」が同じクラスタの左右で潰れる。
  3-02の径を左右2本ずつに振り分けても `left 100%` と出る。
  → 生成図面の評価は **エンジンのビュー領域を正とする `調査/measure_style_placement.py`** で行う
  (計画の `placement.side` と実DXFの寸法線位置の一致も同時に検算する)
- **並置比較の「前」は凍結コピーを持つこと**: `調査/centerline/blind2/` は反証テストの種として
  計画変更のたびに作り直される。比較の基準に使うと「前」が「後」に化ける
  (`調査/style/before/` へ退避してから作り直した)
- BLIND2 5枚の再計画結果(全ゲート合格を維持): 直列連記 **0組 → 9組**(寸法線13本ぶん節約)、
  3-02の輪郭ビューの径 **左100% → 左2:右2**、径と長さが同居する「ビュー×辺」**0件**、
  角度寸法 **0本 → 3本**(3-02: 20度/90度、5-05: 45度)

### ❗OpenDoc6がNoneでも「ActiveDocで拾う」フォールバックは危険【2026-08-09・実害あり】
OpenDoc6 が None(開けていない)のとき `sw.ActiveDoc` は**ユーザーが既に開いていた無関係の
ドキュメント**を返す。そのまま処理を続けて最後に `CloseDoc(title)` したため、
ユーザーが開いていた姉妹プロジェクトの部品(KR195F CASE-OUTER)を閉じてしまった。
- **開いた後は必ずタイトルとファイル名を照合してから処理・CloseDocする**
- **CloseDocは「自分が開いた」ことが確認できたドキュメントに対してのみ呼ぶ**
