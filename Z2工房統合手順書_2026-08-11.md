# Z2工房(SOLIDIFY)統合手順書 — 部品図作成agent を Z2 に融合する

> 作成: 2026-08-11(姉妹リポジトリ「3D CAD Operator Agent」のセッションが、両リポジトリを読んで作成)
> 読者: **このフォルダ(部品図作成agent)で作業するエージェント**。
> 目的: 3D→2D図面生成システムを、Z2 で稼働中の無人工房 **SOLIDIFY**(2D→3D)に融合し、
> 社内の人がブラウザから「3Dモデルを投げると部品図DXFが返ってくる」を使えるようにする。
> 原則: **事実はこの文書とファイルから再構成してよい。解釈は鵜呑みにしない**(両リポジトリ共通の規則)。

---

## 0. 最初に読むもの(着手前・読み取りのみ)

統合先(Z2工房)の正本は姉妹リポジトリにある。**着手前に必ず以下を読むこと**:

| ファイル(姉妹リポジトリ `C:\Users\imaizumi.LINEWORKS-NET\Documents\3D CAD Operator Agent\` 配下) | 何が書いてあるか |
|---|---|
| `CLAUDE.md` | 工房全体の思想・SW COM の罠全集・Z2の制約(❗印の節は全部実測の事故記録) |
| `phase2/実装契約.md` | **SOLIDIFY の仕様の正本(現行 v3.5)**。DBスキーマ・API・状態機械・受け入れ基準・禁止事項 |
| `phase2/引き継ぎ書_Z2移設と社外公開_2026-07-29.md` | Z2 移設の経緯・SSH/公開系の構成 |
| `phase2/引き継ぎ書_2026-08-09_タダノ4部品完納・契約v3.4実機確認・Z2残タスク.md` | 直近の状態(5-1/5-2完了記録つき) |
| `phase2/app/server.py` / `phase2/app/db.py` | UI+API+DB の実体(jobs テーブルに **`kind` 列が既にある**) |
| `phase2/worker/pipeline_real.py` / `phase2/worker/prompts.yaml` | ワーカーの状態機械と `claude -p` プロンプト |
| `phase2/worker/tests/`(test_real_pipeline.py ほか) | テストの流儀(モック・ハーネスの作法) |

このリポジトリ側の前提知識: `CLAUDE.md`(本フォルダ)と `app/workshop.py` の冒頭コメント
(状態遷移と安全規約)。

---

## 1. 統合先(Z2工房)の全体像

- **本番機 Z2**: ホスト `DENKI-ICHIDA` / `192.168.1.96` / ユーザ `t-ichida` / **SolidWorks 2021**(❗開発機は2023)
- リポジトリ: `C:\workshop\3D-CAD-Operator-Agent-Workshop`(git。開発機から bundle を scp して ff する運用)
- Python: `C:\Program Files\Python312\python.exe`(開発機の 3.14 と違う。依存はここに入れる)
- 常駐: `python -m phase2.app --lan`(UI+API・ポート8788)と `python -m phase2.worker --real`(ワーカー)。
  **起動は必ず見張りタスク `SOLIDIFY-Watchdog`(5分間隔)がやる**。手で起こさない(§2.4)
- 画面: 社内 `http://192.168.1.96:8788/` / 社外 `https://solidify.lineworks-local.net/`
  (どちらも合言葉ゲート。API は `x-solidify-passcode` ヘッダ。値は Z2 の環境変数 `SOLIDIFY_PASSCODE` —
  **この値をリポジトリや手順書に書き写さないこと**)
- データ: `data/workshop.db`(SQLite・フェーズ2台帳の実体)/ `data/依頼箱/{job_id}/` / `data/納品箱/{job_id}/`
- 処理の流れ(現行・2D→3D): `queued → reading → verifying → building ⇄ inspecting → (fuda | delivered | failed)`。
  ワーカーが HTTP で claim し、各ステージを **`claude -p`(非対話)** で実行。検査は決定論ランナー
  (`engine/gate_runner.py` 等)が主体
- **SolidWorks の占有は常に同時1**。読解・検証(SW不要)だけ先読み並列する設計
- 運用方針(2026-08-08 ユーザー決定): **Z2 = 部品図(バラシ図)レベルの量産**。難物はセッション対応。
  → 3D→2D の部品図生成は Z2 の守備範囲に合致する

---

## 2. Z2 への接続の仕方

### 2.1 SSH(開発機から)

このPC(imaizumi機)の `~/.ssh/config` に登録済み。**どのフォルダからでも使える**:

```powershell
ssh z2 "hostname"          # → denki-ichida と返れば疎通OK(鍵認証・パスワード不要)
```

**❗2大制約(実測済み・毎回効いてくる)**:
1. **日本語をインラインで渡すと文字化けする** → 複雑な処理は **スクリプトファイルを scp して実行**する:
   ```powershell
   scp mytask.py z2:C:/workshop/mytask.py
   ssh z2 "python C:\workshop\mytask.py"
   # 使い終わったら消す: ssh z2 "Remove-Item C:\workshop\mytask.py -Force -Confirm:0"
   ```
2. **SSH の非対話セッションからは (a) SolidWorks COM が掴めない**(GetActiveObject は同一対話セッション限定)
   **(b) `\\lineworks-sv\Data` 共有が二重ホップで見えない**。
   → **provisioning(pip・clone・非SWテスト)は SSH で可。SW を使う実行は §2.5 の対話タスク経由**。

補足: Z2 の SSH 既定シェルは PowerShell。`&&` は使えない(`;` で繋ぐ)。
ローカル側 PowerShell から二重引用で渡すと `$env:` 等が**ローカルで展開される**ので、
リモートに渡す文字列は単一引用 `'...'` にするか、スクリプトファイル方式にする。

### 2.2 HTTP API(工房の窓口)

```powershell
# 生存確認(合言葉不要)
Invoke-WebRequest -Uri http://192.168.1.96:8788/health -UseBasicParsing
# それ以外の API は合言葉ヘッダが必須(無いと401)
#   Headers: @{ "x-solidify-passcode" = <Z2の SOLIDIFY_PASSCODE の値> }
```

- エージェントによる投入・監視・検収は **LAN の HTTP API(:8788)経由が正**(SW実ビルドに立ち入らない)
- DB を直接読みたいときは **読み取り専用で開く**(実績パターン):
  `sqlite3.connect("file:C:/workshop/3D-CAD-Operator-Agent-Workshop/data/workshop.db?mode=ro", uri=True)`
  **本番DBへの直接書き込みは禁止**(書くのはアプリとワーカーだけ)

### 2.3 リポジトリの移送(git bundle)

GitHub 認証を回避できる **bundle の scp 直送**が実績ある唯一の方法。日本語ファイル名の
一括転送に Windows tar を使うと壊れる(**Python zipfile なら UTF-8 名を保持**)。

```powershell
# 例: このリポジトリを Z2 に新規配置する(§4 M1)
git bundle create "$env:TEMP\drawing_agent.bundle" --all
scp "$env:TEMP\drawing_agent.bundle" z2:C:/workshop/drawing_agent.bundle
ssh z2 "git clone C:\workshop\drawing_agent.bundle C:\workshop\Drawing-Generator-Agent; git -C C:\workshop\Drawing-Generator-Agent checkout main"

# 例: 差分更新(2回目以降)
git bundle create "$env:TEMP\da_update.bundle" <Z2の現在コミット>..main
scp "$env:TEMP\da_update.bundle" z2:C:/workshop/da_update.bundle
ssh z2 "git -C C:\workshop\Drawing-Generator-Agent branch z2-snapshot-<日付>"   # 適用前退避
ssh z2 "git -C C:\workshop\Drawing-Generator-Agent fetch C:\workshop\da_update.bundle main:refs/remotes/bundle/main; git -C C:\workshop\Drawing-Generator-Agent merge --ff-only bundle/main"
```

姉妹リポジトリ(workshop)側の変更も同じ手順(bundle 前提コミットは Z2 の `git log` で確認)。
**ff 適用の前に必ず「実行中ジョブが無い」ことを確認する**(§2.6)。

### 2.4 常駐プロセスの扱い(見張りタスク経由・厳守)

- **SSH から `Start-Process` で本番を起こしてはいけない**(セッションが切れると子ごと死ぬ)
- 再起動の正しい手順: ①実行中ジョブ0を確認(§2.6) → ②`Stop-Process` で app/worker の python を止める →
  ③**何もしないで待つ**。`SOLIDIFY-Watchdog`(5分間隔)のスケジュール発火が蘇生させる →
  ④/health 応答と新しいプロセス開始時刻を確認。
  この「殺して蘇るか」は見張り自体の健全性テストを兼ねる(実測: 2026-08-10 に2回成功)
- `schtasks /run`(手動)での起動確認は**スケジュール発火の生存確認にはならない**

### 2.5 SolidWorks を使う処理を Z2 で動かす方法

- SW COM は**対話セッション必須**。方法は2つ:
  (a) **本番ワーカー経由**(ジョブとして投入する。統合後はこれが正)
  (b) **schtasks の対話タスク**(`/it`)にスクリプトを実行させる(統合前のスパイク用)。
  (b) の例(過去実績の形。タスクは使い捨てで良い):
  ```powershell
  # スクリプトを scp してから:
  ssh z2 "schtasks /create /tn DA-SPIKE /tr 'cmd /c \"\"C:\Program Files\Python312\python.exe\"\" C:\workshop\spike.py > C:\workshop\spike.log 2>&1' /sc once /st 23:59 /it /f"
  ssh z2 "schtasks /run /tn DA-SPIKE"
  # 少し待って: ssh z2 "Get-Content C:\workshop\spike.log -Tail 40"
  # 後片付け:   ssh z2 "schtasks /delete /tn DA-SPIKE /f"
  ```
  ※ 対話タスクは **Z2 に誰かがログオンしているセッション**で動く。SolidWorks が起動していない場合は
  スクリプト側で起動待ち(または人にSW起動を頼む)。**❗ビルド中の SolidWorks のビューに割り込まない**
  (面選択と納品PNGを壊す)— 必ず工房が暇なときに動かす
- **接続と gen_py は必ず `engine/sw_compat.py` の `connect_sw()`/`gen_module()` 経由**
  (Z2=SW2021=ProgID `.29` を自動判別。`.31` や `EnsureModule(...,31,0)` のハードコードは Z2 で死ぬ)

### 2.6 触る前の状態確認(毎回)

```powershell
# 実行中ジョブの確認(fuda/delivered/cancelled/held 以外が居たら作業しない)
# → スクリプト方式で SELECT status, COUNT(*) FROM jobs GROUP BY status を mode=ro で読む
# プロセスと見張りの確認
ssh z2 "Get-CimInstance Win32_Process -Filter \"Name like 'python%'\" | Select-Object ProcessId, CreationDate, CommandLine"
```

---

## 3. 統合アーキテクチャ(推奨設計)

### 3.1 基本方針

**SOLIDIFY に依頼種別 `kind="drawing"`(3D→図面)を追加し、部品図作成agent は Z2 上の
別リポジトリ(`C:\workshop\Drawing-Generator-Agent`)として置き、SOLIDIFY のワーカーが橋渡しする。**

理由:
1. **窓口・認証・台帳・止める操作・見張りを二重に作らない**。SOLIDIFY には合言葉ゲート、
   cloudflared 公開、赤い札(質問票)、承認フロー、v3.3「止める」、裁定台帳が既にある
2. **SolidWorks の同時1占有が単一ワーカーの claim ループで自然に守られる**。
   別サービスに分けると SW の取り合いになる(これが最大の統合リスク)
3. 両リポジトリの CLAUDE.md・契約・テストがそれぞれ育っており、**コードベースの合併はしない**
   (橋渡しは subprocess 境界で疎結合に保つ)

### 3.2 データフロー(kind="drawing" のジョブ)

```
UI(SOLIDIFY): 「図面化(3D→2D)」フォームで STEP + 図番/材質/個数(+任意項目)を投入
  → data/依頼箱/{job_id}/ に model + メタ(jobs.kind="drawing")
ワーカー(claim)                                     ↓SW占有
  段1 準備      : 依頼を Drawing-Generator-Agent/data/依頼箱/<job_id>/ へ複製し
                  依頼.json を生成(workshop.py new 相当)
  段2 計測・候補 : workshop.py scan(決定論: measure_3d → 候補投影 → 候補PNG)【SW】
  段3 向き選択   : claude -p + orientation_prompt.md → choice.json(SW不要)
  段4 投影      : workshop.py scan(project_chosen)【SW】
  段5 作図計画   : claude -p + plan_prompt.md → plan.json(SW不要)
  段6 生成・検査 : workshop.py scan(generate_drawing + ゲート①②/独立検証/レイアウト/中心線)
  → 質問票.md が出たら v3 の blocker(赤い札=fuda)へ変換。合格なら
  納品: DXF + PNG + 解釈レポートを SOLIDIFY の data/納品箱/{job_id}/ へ複製、DB更新、
        人間承認 → 置き場所へ DXF を配達
```

- 段3・段5 は SW を使わないので、**契約v3の先読み(SW占有中に次ジョブの解析)と同じ枠で並列化できる**
  (初版では並列化しなくてよい。まず直列で確実に)
- **リトライ設計**: 不合格→計画やり直しは `workshop.py retry` が既にある。SOLIDIFY 側の
  building⇄inspecting ループと同型(差し戻し回数上限も同じ思想で)
- **止める**: ステージ間で control(cancel/pause/restart)を確認する現行ワーカーの作法に従う。
  claude の kill は起動した本人(ワーカー)がやる

### 3.3 境界の規約(疎結合を保つ)

- SOLIDIFY 側に足すのは **橋渡しモジュール1つ**(例 `phase2/worker/drawing2d.py`)+
  パイプライン分岐 + UI。部品図作成agent の中身は **CLI(workshop.py / claude -p)経由でのみ**呼ぶ
- 部品図作成agent 側は「Z2で動くための修正」だけを行う(§4 M1)。SOLIDIFY の都合で
  engine/ を書き換えない(このリポジトリの安全規約どおり)
- 置き場所は環境変数で渡す(例 `SOLIDIFY_DRAWING_ROOT=C:\workshop\Drawing-Generator-Agent`)。
  **ハードコードしない**(開発機と Z2 でパスが違う)

### 3.4 スコープの注意(3.2 を実装する前に確認)

- **入力は STEP を第一級とする**。Z2 は SW2021 なので、**SW2022以降で保存された SLDPRT は開けない**
  (SolidWorks の互換は後方のみ。開発機2023より条件が厳しい)。UI は STEP 推奨を明記し、
  SLDPRT 投入時は受付時に⚠を出す(参考六面図の指紋⚠と同じ流儀。バージョン判定は
  ヘッダの `VersionHistory` 相当をワーカー段1で行い、開けない版なら早期に赤い札)
- **材質・材質形状・個数は AI に推定させない**(AUTO-001の実測教訓: 3Dから原理的に決まらない)。
  依頼フォームの必須入力にする(現行 `REQUIRED_REQUEST_FIELDS = 図番/材質/個数` と一致)
- **SOLIDIFY の 2D→3D と連結した「DXF→3D→検図用2D」ループは初版のスコープ外**。
  まず「3D投入→部品図DXF」を独立の依頼種別として完成させる(連結は将来の契約改訂で)

---

## 4. 統合手順(マイルストーン)

> 進め方: 各Mの受け入れ基準を満たしてから次へ。**M2(契約承認)より前に SOLIDIFY 側の
> コードを書き始めない**。作業ログと判断は本フォルダの `調査/` と引き継ぎ書に残す。

### M0. 読み込みと疎通(読み取りのみ・半日)

1. §0 の文書を読む。特に姉妹 CLAUDE.md の「❗無人運転(フェーズ2)で判明した罠」
   「夜間無人運転(契約v3)の要点」「Z2」関連の❗節
2. `ssh z2 "hostname"` と `/health` で疎通確認。§2.6 で工房の状態を見る(触らない)
3. 成果物: `調査/z2統合_M0事実メモ.md`(Z2の現在コミット・稼働状態・気づき)

### M1. Z2プロビジョニングと SW2021 スパイク(1日・go/no-go 判定)

**目的: 「このシステムの SW図面API が SW2021 で動くか」を最初に潰す。**
開発は SW2023 で行われており、SW2021 での動作は未実証。ここが no-go なら統合計画全体を
見直すため、**最初にやる**。

1. このリポジトリを bundle で Z2 の `C:\workshop\Drawing-Generator-Agent` へ配置(§2.3)
2. 依存導入(SSHで可): `ssh z2 "python -m pip install ezdxf==1.4.4 matplotlib pillow"`
   (pywin32 は導入済みのはず。`python -c "import win32com"` で確認。
   開発機で使っている import を洗い出して不足があれば足す)
3. スパイクスクリプトを書く(このリポジトリの実コードを呼ぶ最小構成):
   サンプルSTEP 1個(旋盤物でない単純な板物が良い)に対して
   `measure_3d.py 相当 → project_candidates 相当 → generate まで`を1周。
   **検証点(全て開発機で実測済みの罠が SW2021 でも同じ挙動か)**:
   - `sw_compat.connect_sw()` が `.29` を掴む / `gen_module()` が通る
   - STEP取り込み: `GetImportFileData`+`LoadFile4`(OpenDoc6不可の再確認)
   - `CreateDrawViewFromModelView3`(タイトル渡し)/ `IView.Position` の VARIANT 代入と読み戻し
   - `ScaleDecimal` の勝手な2:1 → 明示代入 / `ModelToViewTransform.ArrayData` の列優先変換
   - 図面の DXF 書き出し(AC1015/mm/左下原点)と ezdxf 読み戻し
   - `SetDisplayTangentEdges2` / `QuitDoc` での後始末(ユーザーの開いている文書を閉じない規約)
4. §2.5(b) の対話タスクで実行(工房が暇な時間帯に。事前に §2.6 確認)
5. 成果物: `調査/z2統合_M1_SW2021スパイク.md`(検証点ごとの○×と実測ログ)。
   **1点でも×が出たら回避策を確立するまで M2 に進まない**(版差の罠は姉妹CLAUDE.mdに前例多数)

### M2. 契約変更案の起草と承認(半日+人間待ち)

SOLIDIFY の変更は **実装契約の追補として承認を得てから**行う(確立した手順。
v3.3/v3.4 の変更案ファイルが書式の参考になる: `phase2/契約v3.4変更案_参考六面図と形状照合.md`)。

1. `phase2/契約v3.6変更案_図面化依頼.md` を起草(姉妹リポジトリに置く)。含めるもの:
   - `kind="drawing"` の追加(**DBスキーマ変更は不要**。jobs.kind は既存列。
     図番/材質/個数などのメタは依頼.json/files JSON に入れる — 契約の「契約外のスキーマ変更禁止」に抵触しない形)
   - 状態機械: `queued → measuring → orienting → planning → drafting ⇄ checking → (fuda|delivered|failed)`
     (名前は既存UIの見せ方に合わせて調整可。**cancelled/held/restart/図面差し替えの既存操作が
     drawing でも同じに効くこと**を明記)
   - 納品物: DXF+PNG+解釈レポート。承認は **DXF が納品箱に実在しなければ 409**
     (KR130「モデル無し納品」の教訓。締めはサーバに置く)
   - 配達: 置き場所には **DXF だけ**を置く(現行の「STEPだけ」と同型)
   - 受け入れ基準(テスト名まで): 受付/状態遷移/質問票→fuda/承認409/止める/回帰(㉔同型=
     既存 2D→3D フローのプロンプト・挙動が不変)
2. ユーザー(今泉さん)の承認を得る。**承認前に実装しない**

### M3. ワーカー橋渡しの実装とテスト(2〜3日・開発機で)

1. 姉妹リポジトリに `phase2/worker/drawing2d.py`(橋渡し)+ `pipeline_real.py` の kind 分岐を実装。
   プロンプトは `prompts.yaml` に drawing 用ステージを足す(orientation/plan の指示は
   このリポジトリの `app/prompts/*.md` を **ファイル参照で**読み込ませ、二重管理しない)
2. **部品図作成agent 側の対応**(必要最小限):
   - `workshop.py` の入出力ディレクトリを環境変数で差し替え可能にする(または橋渡しが複製で対応)
   - 質問票.md の書式に topic(短い名詞句・毎回同じ言い回し)を足す — SOLIDIFY の
     **裁定台帳(同じ図番×同じ争点の自動転記)**に乗せるため
   - 終了コード・result.json の契約を明文化(橋渡しが機械読みする)
3. テスト(姉妹リポジトリの流儀に従う):
   - **モックは本番ポート8788を絶対に使わない**(ニセ工房事件の再発防止。ASGI TestClient か別ポート+
     `/health` に `{"mock": true}`)
   - FakeClaude/FakeStep と同型の Fake で drawing パイプラインの状態遷移・fuda化・止める・
     承認409・**既存フロー不変の回帰**を通す
   - SW が要る部分はスタブ化し、実SWテストは M5 の実機一度押しに委ねる
4. 開発機でエンドツーエンド1周(開発機SW2023で。`SOLIDIFY_DRAWING_ROOT` をこのフォルダに向ける)

### M4. UI の実装(1〜2日・開発機で)

`phase2/app/server.py` + `static/` に追加。既存の見た目・作法(名乗り・実況・赤い札)に合わせる:

1. **投入フォーム**: 依頼種別の切替(「図面にする(3D→2D)」)。入力=3Dモデル(STEP推奨)、
   **図番・材質・個数(必須)**、材質形状・尺度・穴注記の表記(φ/キリ)・納品先(任意)。
   SLDPRT には「Z2 は SolidWorks 2021 のため、2022以降で保存した SLDPRT は開けません。STEP を推奨」
   の注意書き
2. **依頼カード**: drawing 用のステージ表示(計測→向き→計画→作図→検査)。
   ライブプレビュー(候補PNG・生成PNG)は既存の実況画像の仕組みに載せる
3. **納品表示**: DXF ダウンロード + プレビューPNG + 解釈レポート。
   様式警告(style_warnings)と「呼び値未確定」等は赤い札ではなく報告欄(notes 相当)に出す
4. **承認**: 既存の承認フロー(名乗り・成果物実在チェック409)を drawing にも効かせる
5. 受け入れテスト: `phase2/app/tests` / `phase2/worker/tests` の流儀で。
   **UI に新しい操作を足したら、テストが通っても本番で自分で一度押す**(pause事件の教訓)を
   M5 でやることを前提に書く

### M5. Z2 展開と実機一度押し(半日)

1. 両リポジトリをコミット → bundle → Z2 へ ff(§2.3。workshop 側は退避ブランチ
   `z2-snapshot-<日付>` を作ってから)。Z2 でテストスイートを回す
2. ワーカー・アプリの再起動は **§2.4 の見張り経由**(実行中ジョブ0を確認してから)
3. **実機一度押し**: 実物の STEP を1件、画面から投入して delivered まで見届ける。
   確認: 全ステージ遷移 / 候補PNGが実況に出る / 質問票が赤い札になる(1件は意図的に
   曖昧な依頼で fuda を出させる)/ 承認 → 置き場所に DXF / 「止める」も1回押す
4. 台帳(`workshop.db`)に記帳されること・`ledger_export` で人間可読に出ることを確認
5. 成果物: 引き継ぎ書(`phase2/引き継ぎ書_<日付>_図面化統合.md`)+ 両リポジトリの CLAUDE.md に知見追記

### M6. 運用開始後の宿題(初版でやらないと決めたこと)

- 段3/段5(SW不要ステージ)の先読み並列化(スループット向上)
- DXF→3D→2D の連結ループ(SOLIDIFY の成果STEPをそのまま図面化に流す)
- ゲート④(人間図面比較)は開発期間の特権なので Z2 には載せない(載せるなら参考データの
  移送と契約整理が要る)

---

## 5. 安全規約(絶対に守る・全て実事故由来)

1. **本番ポート8788をテストで使わない**。ニセAPIは `{"mock": true}` を名乗る
2. **本番DBに直接書かない**。読むときは `mode=ro`
3. **SSH から Start-Process しない**。常駐の起動は見張りタスクに任せる
4. **ff適用・再起動は実行中ジョブ0を確認してから**。適用前に退避ブランチを作る
5. **SW COM に SSH 非対話から触らない**。ビルド中の SolidWorks のビューに割り込まない
6. **合言葉(SOLIDIFY_PASSCODE)・トークン類をコミットしない**
7. **`.31`/`EnsureModule(...,31,0)` を書かない**。必ず sw_compat 経由
8. **エージェントの完了報告を信用しない**(このリポジトリで実際に虚偽報告事故あり)。
   コミット前にファイル実在・関数grep・成果物を最低1点ずつ実地確認
9. **契約承認前に SOLIDIFY 側の実装をしない**。禁止事項(契約外のポート/パス/スキーマ変更、
   既存engine・生成3D・台帳.md への書込み)を守る
10. 納品物のDXF/生成図面はユーザーが eDrawings 等で開いているとロックされる
    (`PermissionError`)。上書き前に閉じてもらう連絡を挟む

## 6. 人間(今泉さん)に確認・依頼が要るもの(先に列挙しておく)

- [ ] 契約v3.6変更案の承認(M2)
- [ ] UI の文言・依頼種別の名前(「図面にする」等)の好み
- [ ] 図面化依頼の納品先(置き場所)の既定。`\\lineworks-sv\...` へ置く運用なら
      二重ホップ制約により対話セッション経由の配達になる(現行3D側と同じ扱いでよいか)
- [ ] M1 スパイク実行の時間帯(工房が暇な時間。SolidWorks の起動もお願いする可能性)
- [ ] 穴注記の既定(現行: φ・全角)と尺度まわりの運用確認(依頼時に選ばせるか、固定か)

---

## 付録A. 今日時点の状態(2026-08-11)

- Z2 の workshop リポジトリ = `1308ac4`(契約v3.5まで適用済み・v3.5コードで稼働中)
- Z2 のジョブ: delivered 21 / fuda 14 / cancelled 6(実行中0)
- このリポジトリ(部品図作成agent)= `94b1a8f` + CLAUDE.md 未コミット変更あり。
  **リモート未設定・Z2未配置**(bundle 移送が初回になる)
- 追跡ファイルはコード・図枠・文書のみ(重量データは gitignore 済み)なので bundle は軽い。
  `荏原トライ調整用/`(参考200MB)と `生成3D(Sample)/` は**Z2 の稼働に不要**(移送しない)

## 付録B. 統合で特に効いてくる既知の罠(出典つき索引)

| 罠 | 出典 |
|---|---|
| SW版差(.31/.29)・ファイル互換は後方のみ | 姉妹CLAUDE.md「SolidWorks API 接続」 |
| STEPは LoadFile4 / GetPathName空 / QuitDoc | 本リポジトリCLAUDE.md「STEP取り込み」 |
| COM はスレッドごとに CoInitialize | 姉妹CLAUDE.md「無人運転で判明した罠」1 |
| SaveBMP は相対パスで黙って失敗 | 同上2 |
| レート制限の分類とサーキットブレーカ | 姉妹CLAUDE.md「夜間無人運転」6 |
| uvicorn keep-alive とポーリング間隔 | 姉妹CLAUDE.md「uvicornの節」 |
| 「止める」は held(claim対象外)へ | 姉妹CLAUDE.md「pause事件」 |
| 承認は成果物実在で409 / 回答はプロンプトへ | 姉妹CLAUDE.md「KR130事件」 |
| sys.stdout 差し替え禁止(reconfigure) | 両リポジトリに実害記録あり |
| ezdxf の描画実体を測る(値一致≠正描画) | 本リポジトリCLAUDE.md「角度寸法の優角事故」 |
| 図面ロック(eDrawings)で PermissionError | 本リポジトリCLAUDE.md 中心線エンジン節 |
