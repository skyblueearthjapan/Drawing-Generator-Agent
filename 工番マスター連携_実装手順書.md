# 工番・納入先・製品名の取得 — 実装手順書（Python版）

対象: `部品図作成agent`（`engine/` `app/` の Python 実装）
作成: 2026-08-17
出典（実装の現物）: `C:\Users\imaizumi.LINEWORKS-NET\Documents\Work Report`（デジタル作業報告書アプリ）

---

## 0. 3行まとめ

1. 工番・納入先・製品名の**大元は社内スプレッドシート「全従業員作業日報マスター」の `工番マスタ` タブ**。
2. そこへ直接アクセスせず、**会社GASの doPost API（トークン1本）を叩けば JSON で丸ごと取れる**。Google認証ライブラリは不要（`urllib` だけで済む）。
3. 現在 **930件**。日次 6:07(JST) 更新。毎回叩かず**必ずキャッシュする**（理由は §4-1、これが最重要）。

---

## 1. データの出どころ

### 1.1 大元のスプレッドシート

| 項目 | 値 |
|---|---|
| スプレッドシートID | `1iu5HoaknlW1W1HheeYv0jqcRq-aY0SyEE2seQd2pHkQ` |
| 通称 | 全従業員作業日報マスター |
| 使うタブ | `工番マスタ`（他に `作業員マスタ` `部署マスタ`） |

`工番マスタ` から読んでいる列は**この5つだけ**です。

```
工番 / 受注先 / 納入先 / 納入先住所 / 品名
```

### 1.2 列名 → APIキーの対応（ここが本題）

**ご依頼の3項目は次のキーで取れます。**

| 欲しいもの | 元シートの列名 | APIのキー | 実データ例 |
|---|---|---|---|
| **工番** | `工番` | `koban` | `LW23125` |
| **納入先** | `納入先` | `nohinSaki` | `住友建機㈱` |
| **製品名** | `品名` | **`kishu`** | `4ton応用機アタッチメントポジショナー` |
| （参考）受注先 | `受注先` | `uketsuke` | `住友建機㈱` |
| （参考）納入先住所 | `納入先住所` | `basho` | `千葉県千葉市稲毛区長沼原町731-1` |

> **注意**: 製品名のキーが `kishu`（機種）である点に注意してください。作業報告書アプリでは「品名」を「装置名／機種」として扱っているため、キー名が `hinmei` ではなく **`kishu`** です。ここを読み違えると空文字が返ってきたように見えます。

このマッピングの実装元は `Work Report\src\Master.js` の `getMasterData()` です。

### 1.3 実データ（2026-08-17 06:07 取り込み時点）

```
件数: 工番 930件 / 作業員 50件 / 部署 7件
```

```json
[
 { "koban": "LW23012", "uketsuke": "ラインワークス", "nohinSaki": "㈱ラインワークス",
   "basho": "", "kishu": "油圧ディスクブレーキ　HB6000" },
 { "koban": "LW23125", "uketsuke": "住友建機㈱",     "nohinSaki": "住友建機㈱",
   "basho": "千葉県千葉市稲毛区長沼原町731-1",
   "kishu": "4ton応用機アタッチメントポジショナー" }
]
```

工番は **`LW` 系と `TS` 系の2系統**あります（`LW`=ラインワークス本体、`TS`=テクノサービス）。

### 1.4 想定用途（実態に合わせて読み替えてください）

生成する部品図の**表題欄**に「工番 / 納入先 / 品名」を差し込む用途を想定しています。工番を1つ与えれば残り2項目が自動で埋まるため、`図枠/` のテンプレートに流し込む値をユーザーに手入力させる必要がなくなります。

**この用途では §4-2 の文字列正規化が実際に問題になります。** 表題欄の枠幅は固定なので、`㈱` の合成文字や品名中の全角スペース、そして**品名が長い**（`4ton応用機アタッチメントポジショナー` = 20文字）ケースの扱いを先に決めてください。

---

## 2. 取得方式の選択

| 方式 | 概要 | Google認証 | 評価 |
|---|---|---|---|
| **A. 会社GAS の doPost API** | トークン付きJSON POST → マスターJSONが返る | **不要** | **推奨** |
| B. 元スプレッドシート直読み | `gspread` 等で `工番マスタ` を読む | 必要（OAuth/SA） | 遅延ゼロだが認証実装が重い |
| C. 作業報告書アプリの `/api/boot` | VPSアプリのREST | 必要（組織Googleログイン） | **不可**。サーバー間呼び出し不可 |

**方式C は使えません。** 作業報告書アプリのVPS側APIは全て Google ログインのセッションCookieで保護されており（`server/auth.js`）、機械的な呼び出し口がありません。

**方式A を採用してください。** 作業報告書アプリ自身がこの経路でマスターを取っています（`server/gas.js`）。`gspread`・`google-auth` 等の追加依存が一切不要で、Python標準ライブラリだけで完結します。

---

## 3. 実装手順（方式A）

### 3.1 前提: URLとトークンの入手

| 環境変数 | 値 |
|---|---|
| `GAS_API_URL` | `https://script.google.com/macros/s/AKfycbyYcfCrvQdWIx_z8KtFUgmvygk_n9TJP91ZZmltf7e8wM1cRBJCw4N1xKeWuSfVtlad/exec` |
| `GAS_API_TOKEN` | **64文字の共有トークン。この手順書には記載しません**（下記手順で取得） |

```bash
# (a) 既に動いている作業報告書アプリのVPSから取る（最速）
ssh lineworks-vps-user "grep '^GAS_API_TOKEN=' /opt/lineworks/.env"

# (b) GAS の管理メニューから表示する
#     マスターのバインド先スプレッドシートを開く
#     → メニュー「作業報告書アプリ」→「VPS連携APIトークンを発行/表示」
```

`.env` に置き、**リポジトリにはコミットしないでください**。このフォルダは Git 管理下（`.git` あり）なので特に注意してください。`.gitignore` に `.env` があるか確認を。

```
GAS_API_URL=https://script.google.com/macros/s/AKfycbyY.../exec
GAS_API_TOKEN=（64文字）
```

### 3.2 呼び出し仕様

**リクエスト** — `POST` / `Content-Type: application/json`

```json
{
  "action": "refreshMaster",
  "token": "（64文字のトークン）",
  "params": {}
}
```

> **トークンはHTTPヘッダではなくJSON body に入れます。** GASの `doPost` はリクエストヘッダを読めないためです。`Authorization` ヘッダを付けても無視されます。

**レスポンス** — 成功時は `result` に包まれて返ります。

```json
{
  "ok": true,
  "result": {
    "kobans": [
      { "koban": "LW23125", "uketsuke": "住友建機㈱", "nohinSaki": "住友建機㈱",
        "basho": "千葉県千葉市稲毛区長沼原町731-1",
        "kishu": "4ton応用機アタッチメントポジショナー" }
    ],
    "staff": [ { "code": "1001", "name": "田村 修二", "dept": "機械設計" } ],
    "depts": ["製缶・溶接","機械加工","組立・塗装","機械設計","電気設計","TSC","業務"],
    "importedAt": "2026-08-17T06:07:04"
  }
}
```

失敗時は `{"ok": false, "error": "..."}`。**必ず `ok` を見てください**（HTTPステータスは失敗でも200で返ります）。

疎通確認だけなら `action` を `"ping"` にすると `{"pong": true, "time": "..."}` が返ります。

### 3.3 サンプルコード

`engine/koban_master.py` として置く想定です。**標準ライブラリのみ・追加依存なし。**

```python
"""
工番マスター取得（会社GAS doPost API 経由）
工番 → 納入先(nohinSaki) / 製品名(kishu) を引く。

外部依存なし（urllib のみ）。requirements.txt の変更は不要。
"""
from __future__ import annotations

import json
import os
import re
import time
import urllib.error
import urllib.request
from pathlib import Path
from typing import Any

GAS_URL = os.environ.get("GAS_API_URL", "")
GAS_TOKEN = os.environ.get("GAS_API_TOKEN", "")

CACHE_PATH = Path(__file__).resolve().parent.parent / "data" / "koban_master.json"
CACHE_TTL_SEC = 12 * 60 * 60  # 12時間（マスターは日次6:07更新）
TIMEOUT_SEC = 45              # GASは遅い

# 送信先を /exec に限定（トークンの誤送信防止）
_URL_RE = re.compile(r"^https://script\.google\.com/macros/s/[\w-]+/exec$")


class MasterError(RuntimeError):
    pass


def gas_call(action: str, params: dict | None = None, retries: int = 2) -> Any:
    if not GAS_URL or not GAS_TOKEN:
        raise MasterError("GAS_API_URL / GAS_API_TOKEN が未設定です")
    if not _URL_RE.match(GAS_URL):
        raise MasterError("GAS_API_URL が不正です（/exec のみ許可）")

    body = json.dumps(
        {"action": action, "token": GAS_TOKEN, "params": params or {}}
    ).encode("utf-8")

    last_err: Exception | None = None
    for attempt in range(retries + 1):
        req = urllib.request.Request(
            GAS_URL, data=body, headers={"Content-Type": "application/json"}, method="POST"
        )
        try:
            # urllib は 302 を自動追従し、GET に切り替える（GASの想定どおり）
            with urllib.request.urlopen(req, timeout=TIMEOUT_SEC) as res:
                text = res.read().decode("utf-8", errors="replace")
            try:
                j = json.loads(text)
            except json.JSONDecodeError:
                # GASが稀にGoogleの警告HTMLを返す。リトライ対象（§4-3）
                raise MasterError(f"GAS応答が不正（HTMLが返った）: {text[:120]}")
            if not j.get("ok"):
                raise MasterError(j.get("error") or "GASエラー")
            return j.get("result")
        except Exception as e:  # noqa: BLE001 - リトライ対象を広く取る
            last_err = e
            if attempt >= retries:
                break
            time.sleep(2 * (attempt + 1))  # 2s, 4s
    raise MasterError(f"マスター取得に失敗しました: {last_err}")


def load_master(force: bool = False) -> dict:
    """マスターを取得（キャッシュ優先）。force=True で強制再取得。"""
    if not force and CACHE_PATH.exists():
        age = time.time() - CACHE_PATH.stat().st_mtime
        if age < CACHE_TTL_SEC:
            return json.loads(CACHE_PATH.read_text(encoding="utf-8"))

    m = gas_call("refreshMaster", {})
    CACHE_PATH.parent.mkdir(parents=True, exist_ok=True)
    CACHE_PATH.write_text(json.dumps(m, ensure_ascii=False), encoding="utf-8")
    return m


def find_koban(koban: str) -> dict | None:
    """工番 → {koban, uketsuke, nohinSaki, basho, kishu} / 見つからなければ None"""
    key = str(koban or "").strip().upper()
    if not key:
        return None
    for k in load_master().get("kobans", []):
        if str(k.get("koban", "")).strip().upper() == key:
            return k  # 先頭一致を採用（§4-4）
    return None


def title_block(koban: str) -> dict:
    """表題欄用の3項目。見つからなくても空文字で必ず返す（描画を止めない）。"""
    hit = find_koban(koban) or {}
    return {
        "koban": hit.get("koban", koban),
        "nohinSaki": hit.get("nohinSaki", ""),
        "kishu": hit.get("kishu", ""),
    }
```

使い方:

```python
from engine.koban_master import find_koban, title_block

hit = find_koban("LW23125")
if hit:
    print("工番   :", hit["koban"])      # LW23125
    print("納入先 :", hit["nohinSaki"])  # 住友建機㈱
    print("製品名 :", hit["kishu"])      # 4ton応用機アタッチメントポジショナー

# 表題欄へ差し込む場合（該当なしでも空文字で返るので描画が止まらない）
tb = title_block("LW23125")
```

### 3.4 キャッシュ方針

- 元データは**日次 6:07(JST)** にしか変わりません。TTL 12時間で十分です。
- JSONは930件で**約140KB**。全件メモリ常駐でも問題ありません。
- バッチで大量の図面を生成するなら、**プロセス起動時に1回 `load_master()` を呼び、以降は辞書を引く**形にしてください。`find_koban()` を図面ごとに呼んでもキャッシュファイルを毎回読み直すので、件数が多いなら次のように辞書化しておくと速いです。

```python
master = load_master()
index = {str(k["koban"]).strip().upper(): k for k in master["kobans"]}  # 重複は後勝ちになる点に注意（§4-4）
```

---

## 4. ハマりどころ（必読）

### 4-1. `refreshMaster` は「再取り込み」を伴う重い処理 ★最重要

`action: "refreshMaster"` は、名前のとおり**元スプレッドシートを読み直してミラーシートを全書き換えしてから**データを返します（`src/Api.js` の `apiRefreshMaster_()` → `importMasters_()` → `getMasterData()`）。

つまり**単なる読み取りではありません**。

- 1回あたり数秒〜十数秒かかる
- 共有スプレッドシートへの**書き込みが発生**する
- 高頻度で叩くと GAS の実行時間クォータに当たる

**図面1枚ごとに呼ぶような実装は絶対に避けてください。** §3.4 のキャッシュを必ず入れること。読み取り専用の窓口が欲しい場合は §5 を参照してください。

### 4-2. 文字列がそのまま使えない — 表題欄では実害あり

実データには次が混ざっています。**表題欄に流し込むなら正規化方針を先に決めてください。**

| 現象 | 実例 | 表題欄での影響 |
|---|---|---|
| 全角の合成文字 `㈱` | `住友建機㈱` `㈱ラインワークス` | フォントに字形が無いと**豆腐（□）になる** |
| 品名の中の**全角スペース** | `油圧ディスクブレーキ　HB6000` | 中央揃えが崩れる |
| 品名が長い | `4ton応用機アタッチメントポジショナー`（20文字） | **枠からあふれる** |
| `納入先住所` が空 | `LW23012` / `LW23013` は `basho` が `""` | 住所欄が空白になる |

DXFで使うフォント（`図枠/` のテンプレートが参照しているスタイル）が `㈱` を持っているか、**実際に1枚出力して確認してください**。持っていない場合は `㈱` → `(株)` の置換が必要です。

```python
def normalize_for_drawing(s: str) -> str:
    """表題欄向けの最小限の正規化。表示用途では原文のままが安全なので使い分ける。"""
    return (str(s or "")
            .replace("㈱", "(株)")
            .replace("㈲", "(有)")
            .replace("　", " ")   # 全角スペース → 半角
            .strip())
```

> **注意**: これは**社名を書き換える処理**です。図面という対外文書に載る以上、置換ルールは独断で決めず確認を取ってください。表示だけなら原文維持が原則です。

### 4-3. GASが稀にHTMLを返す

Googleが警告/ポリシー画面のHTMLを返すことが実際にあります（作業報告書アプリのログで 2026-08-12、08-13 に発生を確認）。恒常的な障害ではなく一時的なものです。

- `json.loads` を必ず例外処理で囲む
- **リトライを入れる**（サンプルコードは2回・指数バックオフ）
- 失敗したら**キャッシュの古いデータで動き続ける**設計にすると、バッチ生成が途中で全滅しません

### 4-4. 同一工番が複数行あり得る

`getMasterData()` は `koban` が空の行を捨てるだけで、**重複排除をしていません**。作業報告書アプリは「先頭で一致した行を採用」しています（`web/app.js` の `masterKoban()`）。同じ方針に揃えるのが無難です。

**辞書内包表記で index を作ると「後勝ち」になり、アプリ本体と結果が食い違います。** 揃えたい場合は次のようにしてください。

```python
index = {}
for k in master["kobans"]:
    key = str(k["koban"]).strip().upper()
    if key not in index:      # 先頭勝ち = アプリ本体と同じ挙動
        index[key] = k
```

### 4-5. 列名一致で読んでいる（列順変更には強い／列名変更には弱い）

取り込みはヘッダ行の**列名で位置を探す**実装です（`src/Master.js` の `importTab_` が `header.indexOf(列名)`）。

- 列を入れ替えても動く
- **列名（`工番` `納入先` `品名`）を1文字でも変えると、その列だけ静かに空文字になる**

`kishu` が全件空などの異常を検知したら警告を出すことを推奨します。

### 4-6. トークンの扱い

- 64文字の共有トークン1本で、Drive保管・**Gmail送信**まで含む全アクションが叩けます。権限分離はされていません。
- したがって**トークンはコミットせず**、`.env` + `.gitignore` で管理してください。
- 漏洩時は GAS メニューから再発行します。**再発行すると作業報告書アプリ側も止まる**ので、VPSの `.env` も同時に更新が必要です（連絡してください）。

---

## 5. 参考: 読み取り専用APIを追加する場合（推奨・未実装）

§4-1 の副作用を避けるには、GAS側に**読み取り専用アクション**を1つ足すのが一番きれいです。`Work Report\src\Api.js` の `doPost` に1行追加するだけです。

```js
// src/Api.js の switch 文に追加
case 'getMaster': out = getMasterData(); break;   // 再取り込みなし・読むだけ
```

反映手順（作業報告書アプリ側の作業）:

```bash
cd "C:\Users\imaizumi.LINEWORKS-NET\Documents\Work Report"
clasp push -f
clasp deploy -i AKfycbyYcfCrvQdWIx_z8KtFUgmvygk_n9TJP91ZZmltf7e8wM1cRBJCw4N1xKeWuSfVtlad --description "getMaster追加"
```

これが入れば、サンプルコードの `gas_call("refreshMaster", {})` を `gas_call("getMaster", {})` に変えるだけで、**軽量・副作用なし**で同じJSONが取れます。

> この変更は本手順書作成時点では**未実施**です。必要になったら作業報告書アプリの担当に依頼してください（数分で終わります）。

---

## 6. 動作確認手順

### 6.1 疎通（トークンが正しいか）

```bash
curl -sL -X POST "$GAS_API_URL" \
  -H "Content-Type: application/json" \
  -d "{\"action\":\"ping\",\"token\":\"$GAS_API_TOKEN\",\"params\":{}}"
# 期待: {"ok":true,"result":{"pong":true,"time":"..."}}
# {"ok":false,"error":"unauthorized"} ならトークン不一致
```

`curl` には **`-L`（リダイレクト追従）が必須**です。GASは302を返します。

### 6.2 実データ取得

```bash
python -c "from engine.koban_master import find_koban; print(find_koban('LW23125'))"
# 期待: {'koban': 'LW23125', 'uketsuke': '住友建機㈱', 'nohinSaki': '住友建機㈱',
#        'basho': '千葉県千葉市稲毛区長沼原町731-1',
#        'kishu': '4ton応用機アタッチメントポジショナー'}
```

Windows のコンソールで文字化けする場合は `set PYTHONUTF8=1` を先に実行してください。

### 6.3 チェックリスト

- [ ] `ping` が `ok:true` を返す
- [ ] `LW23125` で製品名 `4ton応用機アタッチメントポジショナー` が取れる
- [ ] 2回目の呼び出しがキャッシュから返る（GASを叩いていない）
- [ ] 存在しない工番（例 `ZZ99999`）で `None` が返り、落ちない
- [ ] トークン未設定でも `title_block()` が空文字を返し、図面生成が止まらない
- [ ] **`㈱` を含む納入先を実際に1枚出力し、豆腐にならないか確認した**
- [ ] 品名20文字が表題欄からあふれないか確認した
- [ ] `.env` が `.gitignore` に入っている

---

## 7. 参照元ファイル（作業報告書アプリ側の現物）

読む価値がある順です。

| ファイル | 内容 |
|---|---|
| `Work Report\src\Master.js` | 元シートのタブ/列定義、`getMasterData()` のキー変換 **←仕様の正** |
| `Work Report\src\Api.js` | doPost API のアクション一覧・トークン認証 |
| `Work Report\server\gas.js` | サーバー間クライアントの実装例（36行。Pythonへの移植元） |
| `Work Report\web\app.js` | `masterKoban()`（工番検索の先頭勝ちロジック） |
| `Work Report\HANDOVER.md` | 全体構成・運用・ハマりどころ |

以上。
